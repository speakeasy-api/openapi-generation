openapi: 3.1.0
info:
  title: cancel_test
  version: 0.1.0
  summary: A minimal document for testing cancelability
servers:
  - url: http://localhost:12345
security:
  - apiKey: []
paths:
  /ping:
    get:
      operationId: ping
      responses:
        "200":
          description: OK
components:
  securitySchemes:
    apiKey:
      type: apiKey
      name: APIkey
      in: header
