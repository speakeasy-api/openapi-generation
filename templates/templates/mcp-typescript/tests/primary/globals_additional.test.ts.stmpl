/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { SDKCore as SDK } from "../core.js";
import { recordTest } from "./common_helpers.js";
import { globalsGlobalsQueryParameterGet } from "../funcs/globalsGlobalsQueryParameterGet.js";
import { globalsGlobalPathParameterGet } from "../funcs/globalsGlobalPathParameterGet.js";
import { globalsGlobalsHiddenPost } from "../funcs/globalsGlobalsHiddenPost.js";

test("Globals query parameter get uses local", async () => {
  recordTest("globals-query-parameter-get-uses-local");

  const sdk = new SDK({
    globalQueryParam: "test",
  });

  const result = await globalsGlobalsQueryParameterGet(sdk, {
    globalQueryParam: "local",
  });

  if (!result.ok) throw result.error;

{{- if isStrictMCPServer }}
  expect(result.value.StatusCode).toBe(200);
  expect(result.value.res?.args.globalQueryParam).toBe("local");
{{- else }}
  expect(result.value.status).toBe(200);
{{- end }}
});

test("Globals path parameter get uses global", async () => {
  recordTest("globals-path-parameter-get-uses-global");

  const sdk = new SDK({
    globalPathParam: 1,
  });

  const result = await globalsGlobalPathParameterGet(sdk);

  if (!result.ok) throw result.error;

{{- if isStrictMCPServer }}
  expect(result.value.StatusCode).toBe(200);
  expect(result.value.res?.url).toContain("/1");
{{- else }}
  expect(result.value.status).toBe(200);
{{- end }}
});

test("Globals path parameter get uses local", async () => {
  recordTest("globals-path-parameter-get-uses-local");

  const sdk = new SDK({
    globalPathParam: 1,
  });

  const result = await globalsGlobalPathParameterGet(sdk, {
    globalPathParam: 2,
  });

  if (!result.ok) throw result.error;

{{- if isStrictMCPServer }}
  expect(result.value.StatusCode).toBe(200);
  expect(result.value.res?.url).toContain("/2");
{{- else }}
  expect(result.value.status).toBe(200);
{{- end }}
});

test("Globals hidden post", async () => {
  recordTest("globals-hidden-post");

  const sdk = new SDK({
    globalHiddenPathParam: "1",
    globalHiddenQueryParam: "test",
    globalHiddenHeaderParam: "test",
  });

  const result = await globalsGlobalsHiddenPost(sdk, {
    test: "testValue",
    other: 123,
  });

  if (!result.ok) throw result.error;

{{- if isStrictMCPServer }}
  expect(result.value.StatusCode).toBe(200);
{{- else }}
  expect(result.value.status).toBe(200);
{{- end }}
});
