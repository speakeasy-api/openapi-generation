/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { SDKCore as SDK } from "../core.js";
import { recordTest } from "./common_helpers.js";
import { collectionsCollectionsContainingNull } from "../funcs/collectionsCollectionsContainingNull.js";

test("Collections containing null values", async () => {
  recordTest("collections-containing-null");

  const sdk = new SDK();

  const value = {
    requiredArray: ["foo", null],
    requiredMap: { foo: null, bar: 123 },
    optionalArray: ["foo", null],
    optionalMap: { foo: null, bar: 123 },
    arrayOfNullUnion: ["foo", null],
    mapOfNullUnion: { foo: null, bar: 123 },
  };

  const result = await collectionsCollectionsContainingNull(sdk, value);

  if (!result.ok) throw result.error;

{{- if isStrictMCPServer }}
  expect(result.value.StatusCode).toBe(200);
  expect(result.value.object?.json).toEqual(value);
{{- else }}
  expect(result.value.status).toBe(200);
{{- end }}
});
