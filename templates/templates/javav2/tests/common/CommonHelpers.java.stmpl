{{- $okHttp := useOkHttp -}}
/* 
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */
package {{templatePackageName}};

import java.io.ByteArrayInputStream;
import java.io.File;
import java.io.IOException;
import java.io.InputStream;
import java.io.UncheckedIOException;
import java.net.URI;
{{- if $okHttp}}
import {{templatePackageName}}.utils.Headers;
import {{templatePackageName}}.utils.transport.HttpRequest;
import {{templatePackageName}}.utils.transport.HttpResponse;
{{- else}}
import java.net.http.HttpClient.Version;
import java.net.http.HttpHeaders;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
{{- end}}
import java.nio.charset.StandardCharsets;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.nio.file.StandardOpenOption;
import java.util.Collections;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import java.util.Random;
import java.util.stream.Collectors;
import java.util.stream.IntStream;

import javax.net.ssl.SSLSession;
{{- if isJava8}}

import {{templatePackageName}}.utils.Java8Compat;
{{- end}}

public class CommonHelpers {
    
    public static void recordTest(String id) {
        try {
            new File("build").mkdir();
            {{jdkCompat "Files.writeString"}}(Paths.get("build/test-javav2-record.txt"), id + "\n", StandardOpenOption.CREATE,
                    StandardOpenOption.APPEND);
        } catch (IOException e) {
            throw new UncheckedIOException(e);
        }
    }
    
{{- if $okHttp}}

    public static HttpResponse<InputStream> createOkEmptyResponse(HttpRequest r) {
        return new HttpResponse<>(r, 200, new Headers(), createEmptyStream());
    }

    public static HttpResponse<InputStream> createJsonResponse(HttpRequest r, int statusCode, String json) {
        Headers headers = new Headers();
        headers.add("Content-Type", "application/json");
        return new HttpResponse<>(r, statusCode, headers,
                new ByteArrayInputStream(json.getBytes(StandardCharsets.UTF_8)));
    }
{{- else}}

    public static HttpResponse<InputStream> createOkEmptyResponse(HttpRequest r) {
        return new HttpResponse<InputStream>() {

            @Override
            public int statusCode() {
                return 200;
            }

            @Override
            public HttpRequest request() {
                return r;
            }

            @Override
            public Optional<HttpResponse<InputStream>> previousResponse() {
                return Optional.empty();
            }

            @Override
            public HttpHeaders headers() {
                return HttpHeaders.of(Collections.emptyMap(), (k, v) -> true);
            }

            @Override
            public InputStream body() {
                return createEmptyStream();
            }

            @Override
            public Optional<SSLSession> sslSession() {
                return Optional.empty();
            }

            @Override
            public URI uri() {
                return r.uri();
            }

            @Override
            public Version version() {
                return Version.HTTP_1_1;
            }
        };
    }
    
    public static HttpResponse<InputStream> createJsonResponse(HttpRequest r, int statusCode, String json) {
        return new HttpResponse<InputStream>() {

            @Override
            public int statusCode() {
                return statusCode;
            }

            @Override
            public HttpRequest request() {
                return r;
            }

            @Override
            public Optional<HttpResponse<InputStream>> previousResponse() {
                return Optional.empty();
            }

            @Override
            public HttpHeaders headers() {
                return HttpHeaders.of({{jdkCompat "Map.of"}}("Content-Type", {{jdkCompat "List.of"}}("application/json")), (k, v) -> true);
            }

            @Override
            public InputStream body() {
                return new ByteArrayInputStream(json.getBytes(StandardCharsets.UTF_8));
            }

            @Override
            public Optional<SSLSession> sslSession() {
                return Optional.empty();
            }

            @Override
            public URI uri() {
                return r.uri();
            }

            @Override
            public Version version() {
                return Version.HTTP_1_1;
            }
        };
    }
{{- end}}
    
    private static InputStream createEmptyStream() {
        return new InputStream() {

            @Override
            public int read() throws IOException {
                return -1;
            }
            
        };
    }

    private static final Random RANDOM = new Random();

    public static String randomId() {
        String s = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ";
        int idLength = 10;
        return IntStream //
                .range(0, idLength) //
                .mapToObj(i -> String.valueOf(s.charAt(RANDOM.nextInt(s.length())))) //
                .collect(Collectors.joining());
    }
    
}
