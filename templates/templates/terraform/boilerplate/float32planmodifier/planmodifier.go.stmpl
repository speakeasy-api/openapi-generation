package float32planmodifier

import (
	"context"

	"github.com/hashicorp/terraform-plugin-framework/resource/schema/planmodifier"
)
{{- $planModifierName := sanitizeClassName .Local.PlanModifierName}}
{{- $planModifierType := printf "Float32%sPlanModifier" $planModifierName }}
var _ planmodifier.Float32 = {{$planModifierType}}{}

type {{$planModifierType}} struct{}

// Description describes the plan modification in plain text formatting.
func (v {{$planModifierType}}) Description(_ context.Context) string {
	return "TODO: add plan modifier description"
}

// MarkdownDescription describes the plan modification in Markdown formatting.
func (v {{$planModifierType}}) MarkdownDescription(ctx context.Context) string {
	return v.Description(ctx)
}

// Validate performs the plan modification.
func (v {{$planModifierType}}) PlanModifyFloat32(ctx context.Context, req planmodifier.Float32Request, resp *planmodifier.Float32Response) {
	resp.Diagnostics.AddAttributeError(
		req.Path,
		"TODO: implement plan modifier {{$planModifierName}} logic",
		req.Path.String()+": "+v.Description(ctx),
	)
}

func {{$planModifierName}}() planmodifier.Float32 {
	return {{$planModifierType}}{}
}
