package listvalidators

import (
	"context"

	"github.com/hashicorp/terraform-plugin-framework/schema/validator"
)
{{- $validatorName := sanitizeClassName .Local.ValidatorName}}
var _ validator.List = List{{$validatorName}}Validator{}

type List{{$validatorName}}Validator struct{}

// Description describes the validation in plain text formatting.
func (v List{{$validatorName}}Validator) Description(_ context.Context) string {
	return "TODO: add validator description"
}

// MarkdownDescription describes the validation in Markdown formatting.
func (v List{{$validatorName}}Validator) MarkdownDescription(ctx context.Context) string {
	return v.Description(ctx)
}

// Validate performs the validation.
func (v List{{$validatorName}}Validator) ValidateList(ctx context.Context, req validator.ListRequest, resp *validator.ListResponse) {
    resp.Diagnostics.AddAttributeError(
        req.Path,
        "TODO: implement listvalidator {{$validatorName}} logic",
        req.Path.String()+": "+v.Description(ctx),
    )
}


func {{$validatorName}}() validator.List {
	return List{{$validatorName}}Validator{}
}
