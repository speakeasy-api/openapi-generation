package float64validators

import (
	"context"

	"github.com/hashicorp/terraform-plugin-framework/schema/validator"
)
{{- $validatorName := sanitizeClassName .Local.ValidatorName}}
var _ validator.Float64 = Float64{{$validatorName}}Validator{}

type Float64{{$validatorName}}Validator struct{}

// Description describes the validation in plain text formatting.
func (v Float64{{$validatorName}}Validator) Description(_ context.Context) string {
	return "TODO: add validator description"
}

// MarkdownDescription describes the validation in Markdown formatting.
func (v Float64{{$validatorName}}Validator) MarkdownDescription(ctx context.Context) string {
	return v.Description(ctx)
}

// Validate performs the validation.
func (v Float64{{$validatorName}}Validator) ValidateFloat64(ctx context.Context, req validator.Float64Request, resp *validator.Float64Response) {
    resp.Diagnostics.AddAttributeError(
        req.Path,
        "TODO: implement float64validator {{$validatorName}} logic",
        req.Path.String()+": "+v.Description(ctx),
    )
}


func {{$validatorName}}() validator.Float64 {
	return Float64{{$validatorName}}Validator{}
}
