package float32validators

import (
	"context"

	"github.com/hashicorp/terraform-plugin-framework/schema/validator"
)
{{- $validatorName := sanitizeClassName .Local.ValidatorName}}
var _ validator.Float32 = Float32{{$validatorName}}Validator{}

type Float32{{$validatorName}}Validator struct{}

// Description describes the validation in plain text formatting.
func (v Float32{{$validatorName}}Validator) Description(_ context.Context) string {
	return "TODO: add validator description"
}

// MarkdownDescription describes the validation in Markdown formatting.
func (v Float32{{$validatorName}}Validator) MarkdownDescription(ctx context.Context) string {
	return v.Description(ctx)
}

// Validate performs the validation.
func (v Float32{{$validatorName}}Validator) ValidateFloat32(ctx context.Context, req validator.Float32Request, resp *validator.Float32Response) {
    resp.Diagnostics.AddAttributeError(
        req.Path,
        "TODO: implement float32validator {{$validatorName}} logic",
        req.Path.String()+": "+v.Description(ctx),
    )
}

func {{$validatorName}}() validator.Float32 {
	return Float32{{$validatorName}}Validator{}
}
