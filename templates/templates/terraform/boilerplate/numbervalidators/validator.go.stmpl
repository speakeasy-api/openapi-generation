package numbervalidators

import (
	"context"

	"github.com/hashicorp/terraform-plugin-framework/schema/validator"
)
{{- $validatorName := sanitizeClassName .Local.ValidatorName}}
var _ validator.Number = Number{{$validatorName}}Validator{}

type Number{{$validatorName}}Validator struct{}

// Description describes the validation in plain text formatting.
func (v Number{{$validatorName}}Validator) Description(_ context.Context) string {
	return "TODO: add validator description"
}

// MarkdownDescription describes the validation in Markdown formatting.
func (v Number{{$validatorName}}Validator) MarkdownDescription(ctx context.Context) string {
	return v.Description(ctx)
}

// Validate performs the validation.
func (v Number{{$validatorName}}Validator) ValidateNumber(ctx context.Context, req validator.NumberRequest, resp *validator.NumberResponse) {
    resp.Diagnostics.AddAttributeError(
        req.Path,
        "TODO: implement numbervalidator {{$validatorName}} logic",
        req.Path.String()+": "+v.Description(ctx),
    )
}


func {{$validatorName}}() validator.Number {
	return Number{{$validatorName}}Validator{}
}
