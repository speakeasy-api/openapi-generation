{{- recurse 1 -}}
{{- if sdkHasTests .Global.AST -}}
{{addImport "net/http"}}
{{addImport "time"}}
{{addImport "crypto/rand"}}
{{addImport "fmt"}}
{{addSDKPackageImport true}}
package tests

{{genImports}}

func createTestHTTPClient(testName string) {{sanitizeSDKPackageName true}}.HTTPClient {
	return &testClient{
		client: &http.Client{Timeout: 60 * time.Second},
		testName: testName,
		testInstanceID: genTestID(),
	}
}

type testClient struct {
	client *http.Client
	testName string
	testInstanceID string
}

var _ {{sanitizeSDKPackageName true}}.HTTPClient = &testClient{}

func (c *testClient) Do(req *http.Request) (*http.Response, error) {
	req.Header.Set("x-speakeasy-test-name", c.testName)
	req.Header.Set("x-speakeasy-test-instance-id", c.testInstanceID)

	return c.client.Do(req)
}

func genTestID() (string) {
    b := make([]byte, 16)
    rand.Read(b)

    return fmt.Sprintf("%X-%X-%X-%X-%X", b[0:4], b[4:6], b[6:8], b[8:10], b[10:])
}
{{- end}}