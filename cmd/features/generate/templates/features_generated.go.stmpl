// Code generated by cmd/features/generate/main.go. DO NOT EDIT.

package features

func (f Feature) String() string {
    switch(f) {{"{"}}{{range .Local}}
    case {{.SymbolName}}:
        return "{{.ValueName}}"{{end}}
    }   
    return "unknown"
}

func FeatureFromString(s string) Feature {
    switch(s) {{"{"}}{{range .Local}}
    case "{{.ValueName}}":
        return {{.SymbolName}}{{end}}
    }
    return Feature(-1)
}

var featureList = []Feature{{"{"}}{{range .Local}}
    {{.SymbolName}},{{end}}
}

var featureTiers = map[Feature]string{{"{"}}{{range .Local}}
    {{.SymbolName}}: Tier{{.Tier}},{{end}}
}
{{range $tier, $features := getFeaturesByTier .Local}}
var tier{{$tier}}Features = []Feature{{"{"}}{{range $features}}
    {{.SymbolName}},{{end}}
}
{{end}}
var readmeSectionsByFeature = map[Feature][]string{
{{- range $feature, $readmeSections := getReadmeSectionsByFeature .Local}}
{{- if len $readmeSections}}
    {{$feature}}: {{"{"}}{{range $readmeSections}}
        "{{.}}",{{end}}
    },{{end}}    
{{- end}}
}
