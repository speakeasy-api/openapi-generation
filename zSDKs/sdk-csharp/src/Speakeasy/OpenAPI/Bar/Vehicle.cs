//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Speakeasy.OpenAPI.Bar
{
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using Speakeasy.OpenAPI.Bar;
    using Speakeasy.OpenAPI.Utils;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class VehicleType
    {
        private VehicleType(string value)
        {
            Value = value;
        }

        public string Value { get; private set; }

        public static VehicleType Car
        {
            get {
                return new VehicleType("Car");
            }
        }

        public static VehicleType Bike
        {
            get {
                return new VehicleType("Bike");
            }
        }

        public static VehicleType Unknown
        {
            get {
                return new VehicleType("UNKNOWN");
            }
        }

        public override string ToString()
        {
            return Value;
        }
        public static implicit operator String(VehicleType v)
        {
            return v.Value;
        }
        public static VehicleType FromString(string v)
        {
            switch (v)
            {
                case "Car":
                    return Car;
                case "Bike":
                    return Bike;
                case "UNKNOWN":
                    return Unknown;
                default:
                    throw new ArgumentException("Invalid value for VehicleType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((VehicleType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }

    /// <summary>
    /// A non-discriminated union of vehicle types in the bar namespace.
    /// </summary>
    [JsonConverter(typeof(Vehicle.VehicleConverter))]
    public class Vehicle : Speakeasy.OpenAPI.Utils.IOpenUnion
    {
        public Vehicle(VehicleType type)
        {
            Type = type;
        }

        [UnionVariant]
        [SpeakeasyMetadata("form:explode=true")]
        public Car? Car { get; set; }

        [UnionVariant]
        [SpeakeasyMetadata("form:explode=true")]
        public Bike? Bike { get; set; }

        public VehicleType Type { get; set; }

        [JsonIgnore]
        public JToken? UnknownRaw { get; private set; }

        public static Vehicle CreateUnknown(JToken raw)
        {
            return new Vehicle(VehicleType.Unknown) { UnknownRaw = raw };
        }

        public bool IsUnknown()
        {
            return Type.Equals(VehicleType.Unknown);
        }

        bool Speakeasy.OpenAPI.Utils.IOpenUnion.IsUnknown() => IsUnknown();

        public static Vehicle CreateCar(Car car)
        {
            VehicleType typ = VehicleType.Car;

            Vehicle res = new Vehicle(typ);
            res.Car = car;
            return res;
        }
        public static Vehicle CreateBike(Bike bike)
        {
            VehicleType typ = VehicleType.Bike;

            Vehicle res = new Vehicle(typ);
            res.Bike = bike;
            return res;
        }

        public class VehicleConverter : JsonConverter
        {
            public override bool CanConvert(System.Type objectType) => objectType == typeof(Vehicle);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                if (reader.TokenType == JsonToken.Null)
                {
                    return CreateUnknown(JValue.CreateNull());
                }

                JToken jt = JToken.Load(reader);
                var unionCandidates = new UnionCandidatePool<VehicleType>();
                unionCandidates.Add<Car>("Car", 0, VehicleType.Car);
                unionCandidates.Add<Bike>("Bike", 1, VehicleType.Bike);

                var (winner, winnerValue) = unionCandidates.PickBest(
                    jt,
                    Utilities.GetDefaultJsonDeserializerSettings()
                );
                if (winner is null || winnerValue is null)
                {
                    return CreateUnknown(jt);
                }

                var result = new Vehicle(winner.TypeName);
                typeof(Vehicle).GetProperty(winner.PropertyName)!.SetValue(result, winnerValue);
                return result;
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null)
                {
                    throw new InvalidOperationException("Unexpected null JSON value.");
                }

                Vehicle res = (Vehicle)value;

                if (res.IsUnknown())
                {
                    if (res.UnknownRaw != null)
                    {
                        res.UnknownRaw.WriteTo(writer);
                        return;
                    }
                    throw new InvalidOperationException("Unknown union value has no raw payload; construct via CreateUnknown(JToken).");
                }

                if (res.Car != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Car));
                    return;
                }

                if (res.Bike != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Bike));
                    return;
                }

                throw new InvalidOperationException(
                    "Could not serialize union to JSON: no variant value was set. " +
                    "Construct this union using one of the Create* factory methods."
                );
            }
        }
    }
}