//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Speakeasy.OpenAPI
{
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using Speakeasy.OpenAPI.Utils;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class ShapeType
    {
        private ShapeType(string value)
        {
            Value = value;
        }

        public string Value { get; private set; }

        public static ShapeType Circle
        {
            get {
                return new ShapeType("circle");
            }
        }

        public static ShapeType Rectangle
        {
            get {
                return new ShapeType("rectangle");
            }
        }

        public static ShapeType Unknown
        {
            get {
                return new ShapeType("UNKNOWN");
            }
        }

        public override string ToString()
        {
            return Value;
        }
        public static implicit operator String(ShapeType v)
        {
            return v.Value;
        }
        public static ShapeType FromString(string v)
        {
            switch (v)
            {
                case "circle":
                    return Circle;
                case "rectangle":
                    return Rectangle;
                case "UNKNOWN":
                    return Unknown;
                default:
                    throw new ArgumentException("Invalid value for ShapeType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((ShapeType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }

    /// <summary>
    /// A discriminated union of shape types.
    /// </summary>
    [JsonConverter(typeof(Shape.ShapeConverter))]
    public class Shape : Speakeasy.OpenAPI.Utils.IOpenUnion
    {
        public Shape(ShapeType type)
        {
            Type = type;
        }

        [UnionVariant]
        [SpeakeasyMetadata("form:explode=true")]
        public Circle? Circle { get; set; }

        [UnionVariant]
        [SpeakeasyMetadata("form:explode=true")]
        public Rectangle? Rectangle { get; set; }

        public ShapeType Type { get; set; }

        [JsonIgnore]
        public JToken? UnknownRaw { get; private set; }

        public static Shape CreateUnknown(JToken raw)
        {
            return new Shape(ShapeType.Unknown) { UnknownRaw = raw };
        }

        public bool IsUnknown()
        {
            return Type.Equals(ShapeType.Unknown);
        }

        bool Speakeasy.OpenAPI.Utils.IOpenUnion.IsUnknown() => IsUnknown();

        public static Shape CreateCircle(Circle circle)
        {
            ShapeType typ = ShapeType.Circle;
            string typStr = ShapeType.Circle.ToString();
            circle.Type = typStr;
            Shape res = new Shape(typ);
            res.Circle = circle;
            return res;
        }

        public static Shape CreateRectangle(Rectangle rectangle)
        {
            ShapeType typ = ShapeType.Rectangle;
            string typStr = ShapeType.Rectangle.ToString();
            rectangle.Type = typStr;
            Shape res = new Shape(typ);
            res.Rectangle = rectangle;
            return res;
        }

        public class ShapeConverter : JsonConverter
        {
            public override bool CanConvert(System.Type objectType) => objectType == typeof(Shape);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                if (reader.TokenType == JsonToken.Null)
                {
                    return CreateUnknown(JValue.CreateNull());
                }

                JToken token = JToken.Load(reader);
                JObject? jo = token as JObject;
                if (jo == null)
                {
                    return CreateUnknown(token);
                }

                JToken? discriminatorToken = jo.GetValue("type");
                if (discriminatorToken == null || discriminatorToken.Type != JTokenType.String)
                {
                    return CreateUnknown(jo);
                }

                string discriminator = discriminatorToken.ToString();
                if (discriminator == ShapeType.Circle.ToString())
                {
                    Circle circle;
                    try
                    {
                        circle = ResponseBodyDeserializer.DeserializeNotNull<Circle>(jo.ToString());
                    }
                    catch (Exception)
                    {
                        if (!ResponseBodyDeserializer.TryConstructUnvalidated<Circle>(jo.ToString(), out circle))
                        {
                            throw;
                        }
                    }
                    return CreateCircle(circle);
                }
                if (discriminator == ShapeType.Rectangle.ToString())
                {
                    Rectangle rectangle;
                    try
                    {
                        rectangle = ResponseBodyDeserializer.DeserializeNotNull<Rectangle>(jo.ToString());
                    }
                    catch (Exception)
                    {
                        if (!ResponseBodyDeserializer.TryConstructUnvalidated<Rectangle>(jo.ToString(), out rectangle))
                        {
                            throw;
                        }
                    }
                    return CreateRectangle(rectangle);
                }

                return CreateUnknown(jo);
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null)
                {
                    throw new InvalidOperationException("Unexpected null JSON value.");
                }

                Shape res = (Shape)value;

                if (res.IsUnknown())
                {
                    if (res.UnknownRaw != null)
                    {
                        res.UnknownRaw.WriteTo(writer);
                        return;
                    }
                    throw new InvalidOperationException("Unknown union value has no raw payload; construct via CreateUnknown(JToken).");
                }

                if (res.Circle != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Circle));
                    return;
                }

                if (res.Rectangle != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Rectangle));
                    return;
                }

                throw new InvalidOperationException(
                    "Could not serialize union to JSON: no variant value was set. " +
                    "Construct this union using one of the Create* factory methods."
                );
            }
        }
    }
}