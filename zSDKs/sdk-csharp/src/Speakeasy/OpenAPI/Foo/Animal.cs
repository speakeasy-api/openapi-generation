//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Speakeasy.OpenAPI.Foo
{
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using Speakeasy.OpenAPI.Foo;
    using Speakeasy.OpenAPI.Utils;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class AnimalType
    {
        private AnimalType(string value)
        {
            Value = value;
        }

        public string Value { get; private set; }

        public static AnimalType Dog
        {
            get {
                return new AnimalType("dog");
            }
        }

        public static AnimalType Cat
        {
            get {
                return new AnimalType("cat");
            }
        }

        public static AnimalType Unknown
        {
            get {
                return new AnimalType("UNKNOWN");
            }
        }

        public override string ToString()
        {
            return Value;
        }
        public static implicit operator String(AnimalType v)
        {
            return v.Value;
        }
        public static AnimalType FromString(string v)
        {
            switch (v)
            {
                case "dog":
                    return Dog;
                case "cat":
                    return Cat;
                case "UNKNOWN":
                    return Unknown;
                default:
                    throw new ArgumentException("Invalid value for AnimalType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((AnimalType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }

    /// <summary>
    /// A discriminated union of animal types in the foo namespace.
    /// </summary>
    [JsonConverter(typeof(Animal.AnimalConverter))]
    public class Animal : Speakeasy.OpenAPI.Utils.IOpenUnion
    {
        public Animal(AnimalType type)
        {
            Type = type;
        }

        [UnionVariant]
        [SpeakeasyMetadata("form:explode=true")]
        public Dog? Dog { get; set; }

        [UnionVariant]
        [SpeakeasyMetadata("form:explode=true")]
        public Cat? Cat { get; set; }

        public AnimalType Type { get; set; }

        [JsonIgnore]
        public JToken? UnknownRaw { get; private set; }

        public static Animal CreateUnknown(JToken raw)
        {
            return new Animal(AnimalType.Unknown) { UnknownRaw = raw };
        }

        public bool IsUnknown()
        {
            return Type.Equals(AnimalType.Unknown);
        }

        bool Speakeasy.OpenAPI.Utils.IOpenUnion.IsUnknown() => IsUnknown();

        public static Animal CreateDog(Dog dog)
        {
            AnimalType typ = AnimalType.Dog;
            string typStr = AnimalType.Dog.ToString();
            dog.AnimalType = DogAnimalTypeExtension.ToEnum(AnimalType.Dog.ToString());
            Animal res = new Animal(typ);
            res.Dog = dog;
            return res;
        }

        public static Animal CreateCat(Cat cat)
        {
            AnimalType typ = AnimalType.Cat;
            string typStr = AnimalType.Cat.ToString();
            cat.AnimalType = CatAnimalTypeExtension.ToEnum(AnimalType.Cat.ToString());
            Animal res = new Animal(typ);
            res.Cat = cat;
            return res;
        }

        public class AnimalConverter : JsonConverter
        {
            public override bool CanConvert(System.Type objectType) => objectType == typeof(Animal);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                if (reader.TokenType == JsonToken.Null)
                {
                    return CreateUnknown(JValue.CreateNull());
                }

                JToken token = JToken.Load(reader);
                JObject? jo = token as JObject;
                if (jo == null)
                {
                    return CreateUnknown(token);
                }

                JToken? discriminatorToken = jo.GetValue("animalType");
                if (discriminatorToken == null || discriminatorToken.Type != JTokenType.String)
                {
                    return CreateUnknown(jo);
                }

                string discriminator = discriminatorToken.ToString();
                if (discriminator == AnimalType.Dog.ToString())
                {
                    Dog dog;
                    try
                    {
                        dog = ResponseBodyDeserializer.DeserializeNotNull<Dog>(jo.ToString());
                    }
                    catch (Exception)
                    {
                        if (!ResponseBodyDeserializer.TryConstructUnvalidated<Dog>(jo.ToString(), out dog))
                        {
                            throw;
                        }
                    }
                    return CreateDog(dog);
                }
                if (discriminator == AnimalType.Cat.ToString())
                {
                    Cat cat;
                    try
                    {
                        cat = ResponseBodyDeserializer.DeserializeNotNull<Cat>(jo.ToString());
                    }
                    catch (Exception)
                    {
                        if (!ResponseBodyDeserializer.TryConstructUnvalidated<Cat>(jo.ToString(), out cat))
                        {
                            throw;
                        }
                    }
                    return CreateCat(cat);
                }

                return CreateUnknown(jo);
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null)
                {
                    throw new InvalidOperationException("Unexpected null JSON value.");
                }

                Animal res = (Animal)value;

                if (res.IsUnknown())
                {
                    if (res.UnknownRaw != null)
                    {
                        res.UnknownRaw.WriteTo(writer);
                        return;
                    }
                    throw new InvalidOperationException("Unknown union value has no raw payload; construct via CreateUnknown(JToken).");
                }

                if (res.Dog != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Dog));
                    return;
                }

                if (res.Cat != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Cat));
                    return;
                }

                throw new InvalidOperationException(
                    "Could not serialize union to JSON: no variant value was set. " +
                    "Construct this union using one of the Create* factory methods."
                );
            }
        }
    }
}