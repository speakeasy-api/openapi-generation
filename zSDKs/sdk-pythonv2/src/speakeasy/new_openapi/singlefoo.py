"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from . import models as models_, utils as utils_
from ._hooks import HookContext
from .basesdk import AsyncBaseSDK, BaseSDK
from .types import OptionalNullable, UNSET
from .utils import get_security_from_env
from .utils.unmarshal_json_response import unmarshal_json_response
from typing import Mapping, Optional


class SingleFoo(BaseSDK):
    def get_single_namespace_foo_pet(
        self,
        *,
        retries: OptionalNullable[utils_.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.foo.Pet:
        r"""Get Single Namespace Foo Pet

        This endpoint tests using a single component from the foo namespace.
        No import aliasing should be needed since there's no conflict within this group.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)
        req = self._build_request(
            method="GET",
            path="/singleNamespace/foo/pet",
            base_url=base_url,
            url_variables=url_variables,
            request=None,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils_.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getSingleNamespaceFooPet",
                oauth2_scopes=["read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
                tags=None,
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils_.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils_.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models_.foo.Pet, http_res, validate=False)
        if utils_.match_response(http_res, "4XX", "*"):
            http_res_text = utils_.stream_to_text(http_res)
            raise models_.SDKError("API error occurred", http_res, http_res_text)
        if utils_.match_response(http_res, "5XX", "*"):
            http_res_text = utils_.stream_to_text(http_res)
            raise models_.SDKError("API error occurred", http_res, http_res_text)

        raise models_.SDKError("Unexpected response received", http_res)

    def create_single_namespace_foo_pet(
        self,
        *,
        id: str,
        name: str,
        species: str,
        models: Optional[str] = None,
        request: Optional[str] = None,
        operations: Optional[str] = None,
        errors: Optional[str] = None,
        utils: Optional[str] = None,
        retries: OptionalNullable[utils_.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.foo.Pet:
        r"""Create Single Namespace Foo Pet

        This endpoint tests creating a component in the foo namespace.
        No import aliasing should be needed since there's no conflict within this group.

        :param id:
        :param name:
        :param species: The species of the pet (e.g., dog, cat)
        :param models: A field name that often collides
        :param request: The request object to send.
        :param operations: A field name that often collides
        :param errors: A field name that often collides
        :param utils: A field name that often collides
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request_ = models_.foo.Pet(
            id=id,
            name=name,
            species=species,
            models=models,
            request=request,
            operations=operations,
            errors=errors,
            utils=utils,
        )

        req = self._build_request(
            method="POST",
            path="/singleNamespace/foo/pet",
            base_url=base_url,
            url_variables=url_variables,
            request=request_,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils_.serialize_request_body(
                request_, False, False, "json", models_.foo.Pet
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils_.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="createSingleNamespaceFooPet",
                oauth2_scopes=["read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
                tags=None,
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils_.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils_.match_response(http_res, "201", "application/json"):
            return unmarshal_json_response(models_.foo.Pet, http_res, validate=False)
        if utils_.match_response(http_res, "4XX", "*"):
            http_res_text = utils_.stream_to_text(http_res)
            raise models_.SDKError("API error occurred", http_res, http_res_text)
        if utils_.match_response(http_res, "5XX", "*"):
            http_res_text = utils_.stream_to_text(http_res)
            raise models_.SDKError("API error occurred", http_res, http_res_text)

        raise models_.SDKError("Unexpected response received", http_res)


class AsyncSingleFoo(AsyncBaseSDK):
    async def get_single_namespace_foo_pet(
        self,
        *,
        retries: OptionalNullable[utils_.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.foo.Pet:
        r"""Get Single Namespace Foo Pet

        This endpoint tests using a single component from the foo namespace.
        No import aliasing should be needed since there's no conflict within this group.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)
        req = self._build_request_async(
            method="GET",
            path="/singleNamespace/foo/pet",
            base_url=base_url,
            url_variables=url_variables,
            request=None,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils_.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getSingleNamespaceFooPet",
                oauth2_scopes=["read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
                tags=None,
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils_.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils_.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models_.foo.Pet, http_res, validate=False)
        if utils_.match_response(http_res, "4XX", "*"):
            http_res_text = await utils_.stream_to_text_async(http_res)
            raise models_.SDKError("API error occurred", http_res, http_res_text)
        if utils_.match_response(http_res, "5XX", "*"):
            http_res_text = await utils_.stream_to_text_async(http_res)
            raise models_.SDKError("API error occurred", http_res, http_res_text)

        raise models_.SDKError("Unexpected response received", http_res)

    async def create_single_namespace_foo_pet(
        self,
        *,
        id: str,
        name: str,
        species: str,
        models: Optional[str] = None,
        request: Optional[str] = None,
        operations: Optional[str] = None,
        errors: Optional[str] = None,
        utils: Optional[str] = None,
        retries: OptionalNullable[utils_.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.foo.Pet:
        r"""Create Single Namespace Foo Pet

        This endpoint tests creating a component in the foo namespace.
        No import aliasing should be needed since there's no conflict within this group.

        :param id:
        :param name:
        :param species: The species of the pet (e.g., dog, cat)
        :param models: A field name that often collides
        :param request: The request object to send.
        :param operations: A field name that often collides
        :param errors: A field name that often collides
        :param utils: A field name that often collides
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request_ = models_.foo.Pet(
            id=id,
            name=name,
            species=species,
            models=models,
            request=request,
            operations=operations,
            errors=errors,
            utils=utils,
        )

        req = self._build_request_async(
            method="POST",
            path="/singleNamespace/foo/pet",
            base_url=base_url,
            url_variables=url_variables,
            request=request_,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils_.serialize_request_body(
                request_, False, False, "json", models_.foo.Pet
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils_.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="createSingleNamespaceFooPet",
                oauth2_scopes=["read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
                tags=None,
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils_.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils_.match_response(http_res, "201", "application/json"):
            return unmarshal_json_response(models_.foo.Pet, http_res, validate=False)
        if utils_.match_response(http_res, "4XX", "*"):
            http_res_text = await utils_.stream_to_text_async(http_res)
            raise models_.SDKError("API error occurred", http_res, http_res_text)
        if utils_.match_response(http_res, "5XX", "*"):
            http_res_text = await utils_.stream_to_text_async(http_res)
            raise models_.SDKError("API error occurred", http_res, http_res_text)

        raise models_.SDKError("Unexpected response received", http_res)
