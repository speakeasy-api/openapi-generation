## cli group sub-group sub-group-op

An operation at the group's top level

### Synopsis

An operation at the group's top level

```
cli group sub-group sub-group-op [flags]
```

### Examples

```
  cli sub-group sub-group-op
```

### Options

```
  -h, --help   help for sub-group-op
```

### Options inherited from parent commands

```
      --app-id string                    Custom authentication credential
      --bearer-auth string               HTTP Bearer
      --client-credentials string        Client Credentials flow.
      --color string                     Control colored output: auto, always, or never. Respects NO_COLOR and FORCE_COLOR env vars. (default "auto")
      --deprecated-query-param1 string   A deprecated description (env: CLI_DEPRECATED_QUERY_PARAM1)
      --deprecated-query-param2 string   Global deprecated-query-param2 parameter (env: CLI_DEPRECATED_QUERY_PARAM2)
      --header stringArray               Set a custom HTTP request header (format: "Key: Value"). Can be specified multiple times.
      --host-name string                 Server template variable: HostName
      --include-headers                  Include HTTP response headers in the output
      --jq string                        Filter and transform output using a jq expression (e.g., '.name', '.items[] | .id')
      --lone-query-param string          Global lone-query-param parameter (env: CLI_LONE_QUERY_PARAM)
      --mobile-auth string               OAuth2 Password flow, a flow that is too
                                         long to describe in a single line.
      --my-api-key string                API Key
      --no-retries                       Disable automatic retries
      --oauth2 string                    OAuth2 Authorization
      --output-format string             Specify the output format for the command. Options are pretty, json, or yaml. Default is pretty. (default "pretty")
      --password string                  HTTP Basic password
      --port string                      Server template variable: PORT
      --query-param1 string              A long winded, multi-line description (env: CLI_QUERY_PARAM1)
      --retry-config string              Full retry configuration as JSON (e.g., '{"strategy":"backoff","backoff":{"maxElapsedTime":5000}}')
      --retry-connection-errors          Retry on connection errors (EOF, reset, etc.)
      --retry-max-elapsed-time string    Maximum total time for retries (e.g., 30s, 5m)
      --secret string                    Custom authentication credential
      --server string                    Select a server by index (for indexed servers) or name (for named servers)
      --server-url string                Override the default server URL
      --subdomain string                 Server template variable: subdomain
      --timeout string                   HTTP request timeout (e.g., 30s, 5m, 100ms)
      --username string                  HTTP Basic username
      --version string                   Server template variable: version
```

### SEE ALSO

* [cli group sub-group](cli_group_sub-group.md)	 - Operations for sub-group

###### Auto generated by spf13/cobra on 16-Feb-2026
