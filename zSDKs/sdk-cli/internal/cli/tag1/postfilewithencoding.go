// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package tag1

import (
	"fmt"
	"github.com/spf13/cobra"
	"openapi/internal/client"
	"openapi/internal/flagutil"
	"openapi/internal/output"
	"openapi/internal/sdk"
	"openapi/internal/sdk/models/operations"
	"openapi/internal/usage"
)

var postFileWithEncodingCmdMeta = []flagutil.FlagMeta{
	{FlagName: "file", FieldPath: "File", Kind: flagutil.FlagKindFile, Required: true, Description: "The file to upload (supports CSV, PNG, JPEG, or PDF) [required]"},
	{FlagName: "attachment", Shorthand: "a", FieldPath: "Attachment", Kind: flagutil.FlagKindFile, Optional: true, Description: "An optional binary attachment"},
	{FlagName: "file-name", FieldPath: "FileName", Kind: flagutil.FlagKindString, Optional: true, Description: "Optional custom file name"},
	{FlagName: "file-purpose", FieldPath: "FilePurpose", Kind: flagutil.FlagKindString, Optional: true, Description: "Purpose of the file upload"},
	{FlagName: "metadata", Shorthand: "m", FieldPath: "Metadata", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `multipartForm:"name=metadata,json"`, Description: "JSON metadata about the file"},
}

// initPostFileWithEncodingCmd initializes the post-file-with-encoding command.
func initPostFileWithEncodingCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "post-file-with-encoding",
		Short:   "Post File With Encoding",
		Long:    "This endpoint tests the encoding field with multipart/form-data content type.\nAccording to OpenAPI 3.0.3 spec, the encoding field is valid for both\napplication/x-www-form-urlencoded and multipart/* media types.\n\nThis test includes multiple content types for the file field to verify\nhandling of comma-separated content types in encoding.",
		Example: "  cli tag1 post-file-with-encoding --file <path/to/file>",
		Args:    cobra.NoArgs,
		RunE:    runPostFileWithEncodingCmd,
		Aliases: []string{"pfwe"},
		Annotations: map[string]string{
			"speakeasy_operation": "postFileWithEncoding",
		},
	}
	flagutil.RegisterFlags(cmd, postFileWithEncodingCmdMeta)
	if err := flagutil.ValidateMeta[operations.PostFileWithEncodingRequest](postFileWithEncodingCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for post-file-with-encoding: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runPostFileWithEncodingCmd executes the post-file-with-encoding command.
func runPostFileWithEncodingCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	request, err := flagutil.BuildRequest[operations.PostFileWithEncodingRequest](cmd, postFileWithEncodingCmdMeta, "", "")
	if err != nil {
		return flagutil.WithCLIValidation(err)
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Tag1.PostFileWithEncoding(cmd.Context(), *request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
