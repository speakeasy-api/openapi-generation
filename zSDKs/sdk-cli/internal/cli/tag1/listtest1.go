// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package tag1

import (
	"fmt"
	"github.com/spf13/cobra"
	"openapi/internal/client"
	"openapi/internal/flagutil"
	"openapi/internal/output"
	"openapi/internal/sdk/models/operations"
	"openapi/internal/usage"
)

var listTest1CmdMeta = []flagutil.FlagMeta{
	{FlagName: "page", Shorthand: "p", FieldPath: "Page", Kind: flagutil.FlagKindInt64, Required: true, Description: "[required]"},
	{FlagName: "query-param2", FieldPath: "QueryParam2", Kind: flagutil.FlagKindIntEnum, Required: true, EnumValues: []string{"0", "1", "2"}, Description: "An [enum](https://enum.com) \"query parameter\"\nthat is not easily described in a single line.\n\n**Available Values:**\n| Value | Description |\n|-------|-------------|\n| 0     | No data     |\n| 1     | Partial     |\n| 2     | Complete    | (options: 0, 1, 2) [required]"},
	{FlagName: "header-param1", FieldPath: "HeaderParam1", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
}

// initListTest1Cmd initializes the list-test1 command.
func initListTest1Cmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "list-test1",
		Short:   "Get Test1",
		Long:    "This is a \x7b\x7btest\x7d\x7d endpoint.\nIt has a description.",
		Example: "  cli tag1 list-test1 --page 100 --query-param2 1 --header-param1 some example header param",
		Args:    cobra.NoArgs,
		RunE:    runListTest1Cmd,
		Aliases: []string{"lt"},
		Annotations: map[string]string{
			"speakeasy_operation": "listTest1",
		},
	}
	flagutil.RegisterFlags(cmd, listTest1CmdMeta)
	if err := flagutil.ValidateMeta[operations.ListTest1Request](listTest1CmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for list-test1: %w", err)
	}
	cmd.Flags().BoolP("all", "a", false, "Automatically paginate and fetch all results (streams NDJSON for JSON output)")
	cmd.Flags().Int("max-pages", 0, "Maximum number of pages to fetch when using --all (0 = no limit)")
	parent.AddCommand(cmd)
	return nil
}

// runListTest1Cmd executes the list-test1 command.
func runListTest1Cmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	allPages, _ := flagutil.GetBoolFlag(cmd, "all")
	maxPages, _ := flagutil.GetIntFlag(cmd, "max-pages")
	if maxPages < 0 {
		return flagutil.WithCLIValidation(fmt.Errorf("--max-pages must be zero or greater"))
	}
	if flagutil.FlagChanged(cmd, "max-pages") && !allPages {
		return flagutil.WithCLIValidation(fmt.Errorf("--max-pages requires --all"))
	}
	req, err := flagutil.BuildRequest[operations.ListTest1Request](cmd, listTest1CmdMeta, "", "")
	if err != nil {
		return flagutil.WithCLIValidation(err)
	}
	s, err := client.NewClient(cmd, "MyAPIKey", "Option6")
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if serverURL, _ := flagutil.GetStringFlag(cmd, "server-url"); serverURL != "" {
		sdkOpts = append(sdkOpts, operations.WithServerURL(serverURL))
	} else if serverFlag, _ := flagutil.GetStringFlag(cmd, "server"); serverFlag != "" {
		serverURL, ok := operations.ListTest1ServerList[serverFlag]
		if !ok {
			return fmt.Errorf("unknown server %q for this operation", serverFlag)
		}
		sdkOpts = append(sdkOpts, operations.WithServerURL(serverURL))
	}
	if allPages && !client.IsDryRun(cmd) {
		res, err := s.Tag1.ListTest1(cmd.Context(), *req, sdkOpts...)
		if err != nil {
			return output.Error(cmd, err)
		}
		return output.PaginatedResult(cmd, res, "Object", "ResultArray", maxPages, output.PaginationProbe{
			Type:       "offsetLimit",
			CursorKind: "",
			NextCursor: "",
			NextURL:    "",
			Results:    "$.resultArray",
			HasLimit:   false,
		})
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Tag1.ListTest1(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
