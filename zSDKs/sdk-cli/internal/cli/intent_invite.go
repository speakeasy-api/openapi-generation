// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"github.com/spf13/cobra"
	"openapi/internal/client"
	"openapi/internal/flagutil"
	"openapi/internal/interactive"
	"openapi/internal/output"
	"openapi/internal/usage"
	"strings"
)

func InitIntentInvite(parent *cobra.Command) error {
	cmd := &cobra.Command{
		Use:     "invite [email]",
		Short:   "Invite a user by email",
		Long:    "Create a user record from an email address. Full request control:\n\"cli create-user\".\n\nArguments:\n  <email>  Email address to invite",
		Example: "  cli invite someone@example.com\n  cli invite someone@example.com --given-name John",
		Args:    cobra.ArbitraryArgs,
		RunE:    runIntentInviteCmd,
		Annotations: map[string]string{
			"speakeasy_operation":            "createUser",
			flagutil.AnnotationWholeBodyFlag: "body",
			"speakeasy_strict_body_keys":     "true",
			"speakeasy_command_hints":        "{\"CLI_VALIDATION\":[\"Print the exact request schema with --schema\"]}",
		},
	}
	flagutil.RegisterFlags(cmd, createUserCmdMeta)
	flagutil.SetMetaPromptOptional(cmd, createUserCmdMeta, false)
	flagutil.ClearBodyRequirements(cmd, createUserCmdMeta, "")
	_ = flagutil.OverridePromptRequirement(cmd, "email", false, false)
	cmd.Flags().String("body", "", "Request body as JSON (advanced; replaces intent arguments). Can also be provided via stdin; @path reads a file, @- reads stdin to EOF. Use --schema to print the exact JSON Schema.")
	_ = flagutil.AnnotatePromptFlag(cmd, "body", flagutil.PromptFlagSpec{Kind: "json", BodyFlag: true})
	if err := flagutil.AnnotateBodyFields(cmd, createUserCmdMeta, "", "body"); err != nil {
		return fmt.Errorf("annotate body fields for intent invite: %w", err)
	}
	cmd.Flags().Bool("schema", false, "Print the exact JSON Schema of the request body and exit")
	_ = flagutil.AnnotatePromptFlag(cmd, "schema", flagutil.PromptFlagSpec{Kind: "bool", DocSurface: true})
	cmd.Flags().StringP("given-name", "", "", "First name to record")
	_ = flagutil.AnnotatePromptFlag(cmd, "given-name", flagutil.PromptFlagSpec{
		Required: false,
		Kind:     "string",
		Order:    0,
		// A supplied whole body carries this flag's bound key (and the
		// backing operation flag supplies it directly): no prompt then.
		BodySources: []string{"first-name", "body"},
	})
	cmd.Flags().StringP("user-gender", "", "", "Gender to record (e.g. male, female, other)")
	_ = flagutil.AnnotatePromptFlag(cmd, "user-gender", flagutil.PromptFlagSpec{
		Required: false,
		Kind:     "string",
		Order:    1,
		// A supplied whole body carries this flag's bound key (and the
		// backing operation flag supplies it directly): no prompt then.
		BodySources: []string{"gender", "body"},
	})
	if err := interactive.Declare(cmd, interactive.CommandSpec{Args: []interactive.ArgSpec{
		{
			Name: "email", Summary: "Email address to invite",
			Required: true, Variadic: true,
			BodyKey:     "email",
			SatisfiedBy: []string{"email", "body"},
		},
	}}); err != nil {
		return fmt.Errorf("declare interactive arguments for intent invite: %w", err)
	}
	for _, sibling := range parent.Commands() {
		if sibling.Name() == cmd.Name() || sibling.HasAlias(cmd.Name()) {
			return fmt.Errorf("intent command %q collides with the name or alias of an existing %q command; rename the declared command", "invite", sibling.Name())
		}
	}
	parent.AddCommand(cmd)
	return nil
}

var intentInvitePreset = flagutil.PresetMerge{
	Command: "invite",
	Preset:  "{\"associatedIds\":[\"seed-user\"]}",
	Escape:  "cli create-user",
}

func runIntentInviteCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if requested, _ := cmd.Flags().GetBool("schema"); requested {
		return usage.EmitBodySchema(cmd.OutOrStdout(), "createUser")
	}
	bodySurfaces := []string{"body"}
	for _, surface := range bodySurfaces {
		if flagutil.FlagChanged(cmd, surface) {
			if err := flagutil.ResolveBodyFlag(cmd, surface); err != nil {
				return err
			}
		}
	}
	suppliedBodyFlag := ""
	if flagutil.FlagChanged(cmd, "body") {
		suppliedBodyFlag = "body"
	}
	bodySupplied := suppliedBodyFlag != ""
	if !bodySupplied {
		attached, err := flagutil.AttachStdinBody(cmd, "body")
		if err != nil {
			return err
		}
		bodySupplied = attached
	}
	if hint := flagutil.SpacedBoolValueHint(cmd, args); hint != "" && !client.IsJSONDryRun(cmd) && !output.IsMachineMode(cmd) {
		fmt.Fprintln(cmd.ErrOrStderr(), hint)
	}
	if bodySupplied {
		if flagutil.FlagChanged(cmd, "email") {
			if v, _ := flagutil.GetStringFlag(cmd, "email"); v == "" {
				return flagutil.WithCLIValidation(fmt.Errorf("--email must not be empty"))
			}
		}
		if len(args) > 0 {
			if err := flagutil.MergeInputIntoBody(cmd, suppliedBodyFlag, "email", "the <email> argument", strings.Join(args, " ")); err != nil {
				return flagutil.WithCLIValidation(err)
			}
		}
		if flagutil.FlagChanged(cmd, "given-name") {
			v, _ := flagutil.GetStringFlag(cmd, "given-name")
			if err := flagutil.MergeInputIntoBody(cmd, suppliedBodyFlag, "first_name", "--given-name", v); err != nil {
				return flagutil.WithCLIValidation(err)
			}
		}
		if flagutil.FlagChanged(cmd, "user-gender") {
			v, _ := flagutil.GetStringFlag(cmd, "user-gender")
			if err := flagutil.MergeInputIntoBody(cmd, suppliedBodyFlag, "gender", "--user-gender", v); err != nil {
				return flagutil.WithCLIValidation(err)
			}
		}
	}
	if len(args) == 0 && !bodySupplied && !flagutil.FlagChanged(cmd, "email") && !flagutil.FlagChanged(cmd, "body") && !flagutil.FlagChanged(cmd, "given-name") && !flagutil.FlagChanged(cmd, "first-name") && !flagutil.FlagChanged(cmd, "user-gender") && !flagutil.FlagChanged(cmd, "gender") {
		return output.UsageHelpError(cmd, fmt.Errorf("%s", "missing required argument <email> (or pass a full request with --body)"))
	}
	if !bodySupplied {
		body := map[string]any{}
		if err := json.Unmarshal([]byte(intentInvitePreset.Preset), &body); err != nil {
			return err
		}
		if len(args) == 0 && !flagutil.FlagChanged(cmd, "email") {
			return flagutil.WithCLIValidation(fmt.Errorf("missing required argument (or pass a full request with --body)"))
		}
		// An explicitly empty backing-flag value must not stand in for the
		// required positional — nor override a supplied one at the request
		// layer — and bypass required-string validation.
		if flagutil.FlagChanged(cmd, "email") {
			if v, _ := flagutil.GetStringFlag(cmd, "email"); v == "" {
				return flagutil.WithCLIValidation(fmt.Errorf("--email must not be empty"))
			}
		}
		if len(args) > 0 {
			body["email"] = strings.Join(args, " ")
		}
		if flagutil.FlagChanged(cmd, "given-name") {
			v, _ := flagutil.GetStringFlag(cmd, "given-name")
			body["first_name"] = v
		}
		if flagutil.FlagChanged(cmd, "user-gender") {
			v, _ := flagutil.GetStringFlag(cmd, "user-gender")
			body["gender"] = v
		}
		encoded, err := json.Marshal(body)
		if err != nil {
			return err
		}
		if err := cmd.Flags().Set("body", string(encoded)); err != nil {
			return err
		}
	} else {
		for _, surface := range bodySurfaces {
			if !flagutil.FlagChanged(cmd, surface) {
				continue
			}
			raw, _ := flagutil.GetStringFlag(cmd, surface)
			merged, err := flagutil.MergePresetBody(raw, intentInvitePreset)
			if err != nil {
				return err
			}
			if err := cmd.Flags().Set(surface, merged); err != nil {
				return err
			}
		}
	}
	if f := cmd.Flag("jq"); f != nil && !f.Changed {
		_ = f.Value.Set(".id")
	}
	return runCreateUserCmd(cmd, nil)
}
