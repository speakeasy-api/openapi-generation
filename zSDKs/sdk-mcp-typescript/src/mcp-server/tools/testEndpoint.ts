/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { testEndpoint } from "../../funcs/testEndpoint.js";
import { TestEndpointRequest$zodSchema } from "../../models/testendpointop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TestEndpointRequest$zodSchema,
};

export const tool$testEndpoint: ToolDefinition<typeof args> = {
  name: "test-endpoint",
  description: ``,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await testEndpoint(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
