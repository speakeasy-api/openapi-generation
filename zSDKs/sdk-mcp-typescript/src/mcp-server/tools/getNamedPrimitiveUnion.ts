/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getNamedPrimitiveUnion } from "../../funcs/getNamedPrimitiveUnion.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$getNamedPrimitiveUnion: ToolDefinition = {
  name: "get-named-primitive-union",
  description:
    `Test named primitive union options using title and x-speakeasy-name-override`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await getNamedPrimitiveUnion(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
