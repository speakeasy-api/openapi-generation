/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { parenthesesInPathAllowed } from "../../funcs/parenthesesInPathAllowed.js";
import { ParenthesesInPathAllowedRequest$zodSchema } from "../../models/parenthesesinpathallowedop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ParenthesesInPathAllowedRequest$zodSchema,
};

export const tool$parenthesesInPathAllowed: ToolDefinition<typeof args> = {
  name: "parentheses-in-path-allowed",
  description: `A string with {{ double braces }} and { single braces }
and \\{\\{ escaped curlies \\}\\} and \`backticks\`.
and \\\`escaped backticks\\\` and double slashes\\\\
and 'single quotes' and "double quotes".
and  \\'escaped single quotes\\' and \\"escaped double quotes\\".


A string with {{ double braces }} and { single braces }
and \\{\\{ escaped curlies \\}\\} and \`backticks\`.
and \\\`escaped backticks\\\` and double slashes\\\\
and 'single quotes' and "double quotes".
and  \\'escaped single quotes\\' and \\"escaped double quotes\\".
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await parenthesesInPathAllowed(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
