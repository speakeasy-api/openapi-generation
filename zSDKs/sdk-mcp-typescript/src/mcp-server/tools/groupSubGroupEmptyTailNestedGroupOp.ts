/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupSubGroupEmptyTailNestedGroupOp } from "../../funcs/groupSubGroupEmptyTailNestedGroupOp.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$groupSubGroupEmptyTailNestedGroupOp: ToolDefinition = {
  name: "group-sub-group-empty-tail-nested-group-op",
  description: `An operation at the group's deepest level

Notice that 'group.flattened' has no operations.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await groupSubGroupEmptyTailNestedGroupOp(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
