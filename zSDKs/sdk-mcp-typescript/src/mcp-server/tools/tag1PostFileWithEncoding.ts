/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tag1PostFileWithEncoding } from "../../funcs/tag1PostFileWithEncoding.js";
import { PostFileWithEncodingRequest$zodSchema } from "../../models/postfilewithencodingop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostFileWithEncodingRequest$zodSchema,
};

export const tool$tag1PostFileWithEncoding: ToolDefinition<typeof args> = {
  name: "tag1-post-file-with-encoding",
  description: `Post File With Encoding

This endpoint tests the encoding field with multipart/form-data content type.
According to OpenAPI 3.0.3 spec, the encoding field is valid for both
application/x-www-form-urlencoded and multipart/* media types.

This test includes multiple content types for the file field to verify
handling of comma-separated content types in encoding.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tag1PostFileWithEncoding(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
