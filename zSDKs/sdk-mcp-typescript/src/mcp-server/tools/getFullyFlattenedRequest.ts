/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getFullyFlattenedRequest } from "../../funcs/getFullyFlattenedRequest.js";
import { GetFullyFlattenedRequestRequest$zodSchema } from "../../models/getfullyflattenedrequestop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetFullyFlattenedRequestRequest$zodSchema,
};

export const tool$getFullyFlattenedRequest: ToolDefinition<typeof args> = {
  name: "get-fully-flattened-request",
  description: ``,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await getFullyFlattenedRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
