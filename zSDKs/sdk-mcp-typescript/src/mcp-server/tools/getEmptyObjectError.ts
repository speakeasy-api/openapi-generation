/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getEmptyObjectError } from "../../funcs/getEmptyObjectError.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$getEmptyObjectError: ToolDefinition = {
  name: "get-empty-object-error",
  description: `Get Empty Object Error

This endpoint tests the behavior when an error response has an empty object schema.
This is a regression test for GEN-2213.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await getEmptyObjectError(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
