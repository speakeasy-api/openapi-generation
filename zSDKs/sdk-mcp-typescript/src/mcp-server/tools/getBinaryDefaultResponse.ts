/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getBinaryDefaultResponse } from "../../funcs/getBinaryDefaultResponse.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$getBinaryDefaultResponse: ToolDefinition = {
  name: "get-binary-default-response",
  description: ``,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await getBinaryDefaultResponse(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
