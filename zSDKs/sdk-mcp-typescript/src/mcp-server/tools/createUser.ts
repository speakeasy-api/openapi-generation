/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { createUser } from "../../funcs/createUser.js";
import { BaseUser$zodSchema } from "../../models/baseuser.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: BaseUser$zodSchema,
};

export const tool$createUser: ToolDefinition<typeof args> = {
  name: "create-user",
  description: `Create User

Creates a new user in the system. Multiple named examples demonstrate
different pairing scenarios for documentation generation.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await createUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
