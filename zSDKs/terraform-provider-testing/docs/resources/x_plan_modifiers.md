---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "testing_x_plan_modifiers Resource - terraform-provider-testing"
subcategory: ""
description: |-
  XPlanModifiers Resource
---

# testing_x_plan_modifiers (Resource)

XPlanModifiers Resource

## Example Usage

```terraform
resource "testing_x_plan_modifiers" "my_xplanmodifiers" {
  any     = "{ \"see\": \"documentation\" }"
  bool    = true
  float32 = 1.21
  float64 = 4.15
  int32   = 7
  int64   = 2
  list = [
    "..."
  ]
  list_nested = [
    {
      list_nested_string = "...my_list_nested_string..."
    }
  ]
  map = {
    key = jsonencode("value")
  }
  object = {
    object_string = "...my_object_string..."
  }
  set = [
    "..."
  ]
  set_nested = [
    {
      set_nested_string = "...my_set_nested_string..."
    }
  ]
  string = "...my_string..."
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `any` (String) Parsed as JSON.
- `bool` (Boolean)
- `float32` (Number)
- `float64` (Number)
- `int32` (Number)
- `int64` (Number)
- `list` (List of String)
- `list_nested` (Attributes List) (see [below for nested schema](#nestedatt--list_nested))
- `map` (Map of String)
- `object` (Attributes) (see [below for nested schema](#nestedatt--object))
- `set` (Set of String)
- `set_nested` (Attributes Set) (see [below for nested schema](#nestedatt--set_nested))
- `string` (String)

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedatt--list_nested"></a>
### Nested Schema for `list_nested`

Optional:

- `list_nested_string` (String)


<a id="nestedatt--object"></a>
### Nested Schema for `object`

Optional:

- `object_string` (String)


<a id="nestedatt--set_nested"></a>
### Nested Schema for `set_nested`

Optional:

- `set_nested_string` (String)

## Import

Import is supported using the following syntax:

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = testing_x_plan_modifiers.my_testing_x_plan_modifiers
  id = "..."
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import testing_x_plan_modifiers.my_testing_x_plan_modifiers "..."
```
