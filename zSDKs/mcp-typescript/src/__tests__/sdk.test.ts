/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assert, expect, it, test } from "vitest";
import { SDKCore as SDK } from "../core.js";
import { createUser } from "../funcs/createUser.js";
import { deleteUser } from "../funcs/deleteUser.js";
import { getBinaryDefaultResponse } from "../funcs/getBinaryDefaultResponse.js";
import { getFullyFlattenedRequest } from "../funcs/getFullyFlattenedRequest.js";
import { getNestedIntegerString } from "../funcs/getNestedIntegerString.js";
import { getPolymorphism } from "../funcs/getPolymorphism.js";
import { getRequestBodyFlattenedAway } from "../funcs/getRequestBodyFlattenedAway.js";
import { getUnionErrors } from "../funcs/getUnionErrors.js";
import { getUser } from "../funcs/getUser.js";
import { postFile } from "../funcs/postFile.js";
import { updateUser } from "../funcs/updateUser.js";
import { assertDefined } from "./assertions.js";
import { fileToBase64 } from "./files.js";
import { createTestHTTPClient } from "./testclient.js";
import { authenticate, userSetup, userTeardown } from "./testhelpers.js";

it.skip("Sdk Test Endpoint", async () => {
  assert.fail(
    "incomplete test found please make sure to address the following errors: [`workflow step testEndpoint.test referencing operation testEndpoint is missing required request body`, `workflow step testEndpoint.test referencing operation testEndpoint missing required path parameter testName`]",
  );
});

test("Sdk Get User Test With Status Code Only", async () => {
  const testHttpClient = createTestHTTPClient("getUser-testWithStatusCodeOnly");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const result = await getUser(sdk, {
    id: "test123",
  });

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
});

test("Sdk Get User Test With Response Body", async () => {
  const testHttpClient = createTestHTTPClient("getUser-testWithResponseBody");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const result = await getUser(sdk, {
    id: "test123",
  });

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
});

test("Sdk Get User Test With Response Body Fields", async () => {
  const testHttpClient = createTestHTTPClient(
    "getUser-testWithResponseBodyFields",
  );

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const result = await getUser(sdk, {
    id: "test123",
  });

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
});

test("Sdk Get Fully Flattened Request Named Example That Isnt Matched Across Different Examples", async () => {
  const testHttpClient = createTestHTTPClient(
    "getFullyFlattenedRequest-namedExampleThatIsntMatchedAcrossDifferentExamples",
  );

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_URL"] ?? "",
    },
  });

  const result = await getFullyFlattenedRequest(sdk, {
    lang: "en",
    RequestBody: {
      name: "<value>",
    },
  });

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
});

test("Sdk Post File", async () => {
  const testHttpClient = createTestHTTPClient("postFile");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const result = await postFile(sdk, {
    file: {
      fileName: "postfiletest.txt",
      content: await fileToBase64(".speakeasy/testfiles/postfiletest.txt"),
    },
  });

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
});

test("Sdk Get Polymorphism", async () => {
  const testHttpClient = createTestHTTPClient("getPolymorphism");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const result = await getPolymorphism(sdk);

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
});

test("Sdk Get Union Errors", async () => {
  const testHttpClient = createTestHTTPClient("getUnionErrors");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const result = await getUnionErrors(sdk, {
    page: 12,
  });

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
});

test("Sdk Get Request Body Flattened Away", async () => {
  const testHttpClient = createTestHTTPClient("getRequestBodyFlattenedAway");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
      loneQueryParam: "<value>",
    },
  });

  const result = await getRequestBodyFlattenedAway(sdk);

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
});

test("Sdk Get User Success", async () => {
  const testHttpClient = createTestHTTPClient("getUser-success");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const result = await getUser(sdk, {
    id: "<id>",
  });

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
});

it.skip("Sdk Chat", async () => {
  assert.fail(
    "incomplete test found please make sure to address the following errors: [`workflow step chat.test referencing operation chat not found in document`]",
  );
});

test("Sdk Get Binary Default Response", async () => {
  const testHttpClient = createTestHTTPClient("getBinaryDefaultResponse");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const result = await getBinaryDefaultResponse(sdk);

  if (!result.ok) throw result.error;
  expect([200]).not.toContain(result.value.status);
});

test("Sdk User Lifecycle", async () => {
  const testHttpClient = createTestHTTPClient("user-lifecycle");

  const authenticate_outputs = await authenticate();
  expect(authenticate_outputs).toBeDefined();
  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
      security: {
        UserPassAuth: {
          username: "<USERNAME>",
          password: "<PASSWORD>",
        },
      },
    },
  });

  const create_result = await createUser(sdk, {
    email: "Trystan_Crooks@hotmail.com",
    first_name: "Trystan",
    last_name: "Crooks",
    age: 32,
    postal_code: "94110",
    metadata: {
      allergies: "none",
      AdditionalProperties: {
        "color": "red",
        "height": "182",
        "weight": "77",
        "is_smoking": "true",
      },
    },
  });

  if (!create_result.ok) throw create_result.error;
  expect(create_result.value.status).toBe(200);

  const get_result = await getUser(sdk, {
    id: assertDefined((create_result.value as any).User?.id),
  });

  if (!get_result.ok) throw get_result.error;
  expect(get_result.value.status).toBe(200);

  const update_result = await updateUser(sdk, {
    id: assertDefined((create_result.value as any).User?.id),
    User: assertDefined((get_result.value as any).User),
  });

  if (!update_result.ok) throw update_result.error;
  expect(update_result.value.status).toBe(200);

  const updateAgain_result = await updateUser(sdk, {
    id: assertDefined((create_result.value as any).User?.id),
    User: {
      id: assertDefined((create_result.value as any).User?.id),
      email: assertDefined((update_result.value as any).User?.email),
      first_name: (update_result.value as any).User?.first_name,
      last_name: (update_result.value as any).User?.last_name,
      age: 33,
      postal_code: "94110",
      metadata: (update_result.value as any).User?.metadata,
    },
  });

  if (!updateAgain_result.ok) throw updateAgain_result.error;
  expect(updateAgain_result.value.status).toBe(200);

  const delete_result = await deleteUser(sdk, {
    id: assertDefined((create_result.value as any).User?.id),
  });

  if (!delete_result.ok) throw delete_result.error;
  expect(delete_result.value.status).toBe(200);
});

test("Sdk Update User", async () => {
  const testHttpClient = createTestHTTPClient("updateUser");

  const setup_outputs = await userSetup();
  expect(setup_outputs).toBeDefined();
  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const result = await updateUser(sdk, {
    id: setup_outputs.user.id,
    User: setup_outputs.user,
  });

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
  await userTeardown({
    id: setup_outputs.user.id,
  });
});

test("Sdk Get Nested Integer String", async () => {
  const testHttpClient = createTestHTTPClient("getNestedIntegerString");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const result = await getNestedIntegerString(sdk);

  if (!result.ok) throw result.error;
  expect(result.value.status).toBe(200);
});
