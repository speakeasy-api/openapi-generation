/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect } from "vitest";
import { SDKCore as SDK } from "../core.js";
import { createUser } from "../funcs/createUser.js";
import { deleteUser } from "../funcs/deleteUser.js";
import { login } from "../funcs/login.js";
import { validate } from "../funcs/validate.js";
import { createTestHTTPClient } from "./testclient.js";

export async function authenticate(): Promise<any> {
  const testHttpClient = createTestHTTPClient("authenticate");

  let sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
      security: {
        Option4: {
          appId: "app-speakeasy-123",
          secret: "MTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTI",
        },
      },
    },
  });

  const login_result = await login(sdk);

  if (!login_result.ok) throw login_result.error;
  expect(login_result.value.status).toBe(200);
  sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
      security: {
        UserPassAuth: {
          username: "<USERNAME>",
          password: "<PASSWORD>",
        },
      },
    },
  });

  const validate_result = await validate(sdk);

  if (!validate_result.ok) throw validate_result.error;
  expect(validate_result.value.status).toBe(200);

  return {
    token: (login_result.value as any).object?.token,
  };
}

export async function userSetup(): Promise<any> {
  const testHttpClient = createTestHTTPClient("userSetup");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const create_result = await createUser(sdk, {
    email: "Trystan_Crooks@hotmail.com",
    first_name: "Trystan",
    last_name: "Crooks",
    age: 32,
    postal_code: "94110",
    metadata: {
      allergies: "none",
      AdditionalProperties: {
        "color": "red",
        "height": "182",
        "weight": "77",
        "is_smoking": "true",
      },
    },
  });

  if (!create_result.ok) throw create_result.error;
  expect(create_result.value.status).toBe(200);

  return {
    user: (create_result.value as any).User,
  };
}

export async function userTeardown(inputs: any) {
  const testHttpClient = createTestHTTPClient("userTeardown");

  const sdk = new SDK({
    httpClient: testHttpClient,
    ...{
      serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    },
  });

  const delete_result = await deleteUser(sdk, {
    id: inputs.id,
  });

  if (!delete_result.ok) throw delete_result.error;
  expect(delete_result.value.status).toBe(200);
}
