/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { Circle, Circle$zodSchema } from "./circle.js";
import { Rectangle, Rectangle$zodSchema } from "./rectangle.js";

/**
 * A discriminated union of shape types
 */
export type Shape =
  | (Rectangle & { type: "rectangle" })
  | (Circle & { type: "circle" });

export const Shape$zodSchema: z.ZodType<Shape> = z.union([
  Rectangle$zodSchema.and(
    z.object({
      type: z.literal("rectangle"),
    }).transform((v) => ({ type: v.type })),
  ),
  Circle$zodSchema.and(
    z.object({
      type: z.literal("circle"),
    }).transform((v) => ({ type: v.type })),
  ),
]).describe("A discriminated union of shape types");
