/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * Escalation configuration with string-encoded integer expiration
 */
export type Escalation = {
  enabled: boolean;
  expiration: number;
  fallbackApprovers: Array<string>;
};

export const Escalation$zodSchema: z.ZodType<Escalation> = z.object({
  enabled: z.boolean(),
  expiration: z.union([z.string(), z.int()]).transform(v =>
    typeof v === "string" ? parseInt(v, 10) : v
  ).describe("Expiration in seconds, encoded as a string in the API response"),
  fallbackApprovers: z.array(z.string()),
}).describe("Escalation configuration with string-encoded integer expiration");
