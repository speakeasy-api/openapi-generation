/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { buildCommand } from "@stricli/core";
import { numberParser } from "@stricli/core";
import * as z from "zod";
import { ServerPORT } from "../../../lib/config.js";
import { consoleLoggerLevels } from "../../console-logger.js";

export const startCommand = buildCommand({
  loader: async () => {
    const { main } = await import("./impl.js");
    return main;
  },
  parameters: {
    flags: {
      transport: {
        kind: "enum",
        brief: "The transport to use for communicating with the server",
        default: "stdio",
        values: ["stdio", "sse"],
      },
      port: {
        kind: "parsed",
        brief: "The port to use when the SSE transport is enabled",
        default: "2718",
        parse: (val: string) =>
          z.coerce.number().int().gte(0).lt(65536).parse(val),
      },
      tool: {
        kind: "parsed",
        brief: "Specify tools to mount on the server",
        optional: true,
        variadic: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      mode: {
        kind: "enum",
        brief:
          "Server mode (dynamic: expose list_tools, describe_tool_input, and execute_tool instead of individual tools)",
        values: ["dynamic"],
        optional: true,
      },
      "tool-annotations": {
        kind: "parsed",
        brief:
          "Filter tools by annotations (comma-separated: readOnly, destructive, idempotent, openWorld). Listed = required true, unlisted = required false.",
        optional: true,
        parse: (value) => value.split(",").map(s => s.trim()),
      },
      username: {
        kind: "parsed",
        brief: "Sets the Username auth field for the API",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      password: {
        kind: "parsed",
        brief: "Sets the Password auth field for the API",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      "bearer-auth": {
        kind: "parsed",
        brief: "Sets the bearerAuth auth field for the API",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      "my-api-key": {
        kind: "parsed",
        brief: "Sets the MyApiKey auth field for the API",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      oauth2: {
        kind: "parsed",
        brief: "Sets the oauth2 auth field for the API",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      "app-id": {
        kind: "parsed",
        brief: "Sets the appId auth field for the API",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      secret: {
        kind: "parsed",
        brief: "Sets the secret auth field for the API",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      "mobile-auth": {
        kind: "parsed",
        brief: "Sets the mobileAuth auth field for the API",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      "client-credentials": {
        kind: "parsed",
        brief: "Sets the clientCredentials auth field for the API",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      "query-param1": {
        kind: "parsed",
        brief:
          "Allows setting the queryParam1 parameter for all supported operations",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      "deprecated-query-param1": {
        kind: "parsed",
        brief:
          "Allows setting the deprecatedQueryParam1 parameter for all supported operations",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      "deprecated-query-param2": {
        kind: "parsed",
        brief:
          "Allows setting the deprecatedQueryParam2 parameter for all supported operations",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      "lone-query-param": {
        kind: "parsed",
        brief:
          "Allows setting the loneQueryParam parameter for all supported operations",
        optional: true,
        parse: (value) => {
          return z.string().parse(value);
        },
      },
      "server-url": {
        kind: "parsed",
        brief: "Overrides the default server URL used by the SDK",
        optional: true,
        parse: (value) => new URL(value).toString(),
      },
      "server-index": {
        kind: "parsed",
        brief: "Selects a predefined server used by the SDK",
        optional: true,
        parse: numberParser,
      },
      subdomain: {
        kind: "parsed",
        brief: "Sets the subdomain variable for url substitution",
        optional: true,
        parse: (value) => value,
      },
      "api-version": {
        kind: "parsed",
        brief: "Sets the version variable for url substitution",
        optional: true,
        parse: (value) => value,
      },
      "api-host-name": {
        kind: "parsed",
        brief: "Sets the HostName variable for url substitution",
        optional: true,
        parse: (value) => value,
      },
      "api-port": {
        kind: "enum",
        brief: "Sets the PORT variable for url substitution",
        optional: true,
        values: Object.values(ServerPORT) as Array<ServerPORT>,
      },
      "log-level": {
        kind: "enum",
        brief: "The log level to use for the server",
        default: "info",
        values: consoleLoggerLevels,
      },
      env: {
        kind: "parsed",
        brief: "Environment variables made available to the server",
        optional: true,
        variadic: true,
        parse: (val: string) => {
          const sepIdx = val.indexOf("=");
          if (sepIdx === -1) {
            throw new Error("Invalid environment variable format");
          }

          const key = val.slice(0, sepIdx);
          const value = val.slice(sepIdx + 1);

          return [
            z.string().nonempty({
              message: "Environment variable key must be a non-empty string",
            }).parse(key),
            z.string().nonempty({
              message: "Environment variable value must be a non-empty string",
            }).parse(value),
          ] satisfies [string, string];
        },
      },
    },
  },
  docs: {
    brief: "Run the Model Context Protocol server",
  },
});
