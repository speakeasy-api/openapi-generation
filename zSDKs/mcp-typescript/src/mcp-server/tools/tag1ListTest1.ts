/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tag1ListTest1 } from "../../funcs/tag1ListTest1.js";
import { ListTest1Request$zodSchema } from "../../models/listtest1op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListTest1Request$zodSchema,
};

export const tool$tag1ListTest1: ToolDefinition<typeof args> = {
  name: "tag1-list-test1",
  description: `Get Test1

This is a {{test}} endpoint.
It has a description.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tag1ListTest1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
