/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { createWithUnion } from "../../funcs/createWithUnion.js";
import { CreateWithUnionRequest$zodSchema } from "../../models/createwithunionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateWithUnionRequest$zodSchema,
};

export const tool$createWithUnion: ToolDefinition<typeof args> = {
  name: "create-with-union",
  description: `Create with discriminated union request body

Test CLI generation for discriminated unions with dot-notation flags`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await createWithUnion(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
