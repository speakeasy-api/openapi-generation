/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getDuplicateExportCollision } from "../../funcs/getDuplicateExportCollision.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$getDuplicateExportCollision: ToolDefinition = {
  name: "get-duplicate-export-collision",
  description:
    `Tests that a spec-defined error type colliding with a built-in SDK error name does not cause TS2308`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await getDuplicateExportCollision(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
