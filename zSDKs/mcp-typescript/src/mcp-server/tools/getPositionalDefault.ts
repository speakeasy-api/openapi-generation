/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getPositionalDefault } from "../../funcs/getPositionalDefault.js";
import { GetPositionalDefaultRequest$zodSchema } from "../../models/getpositionaldefaultop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPositionalDefaultRequest$zodSchema,
};

export const tool$getPositionalDefault: ToolDefinition<typeof args> = {
  name: "get-positional-default",
  description: `Get an item with a default path identifier

Check that a default-backed path parameter can be supplied as an argument or omitted.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await getPositionalDefault(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
