/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postFile } from "../../funcs/postFile.js";
import { PostFileRequest$zodSchema } from "../../models/postfileop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostFileRequest$zodSchema,
};

export const tool$postFile: ToolDefinition<typeof args> = {
  name: "post-file",
  description: `Post File

This is a test endpoint.
It has a description.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await postFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
