/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getUnionErrors } from "../../funcs/getUnionErrors.js";
import { GetUnionErrorsRequest$zodSchema } from "../../models/getunionerrorsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetUnionErrorsRequest$zodSchema,
};

export const tool$getUnionErrors: ToolDefinition<typeof args> = {
  name: "get-union-errors",
  description: ``,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await getUnionErrors(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
