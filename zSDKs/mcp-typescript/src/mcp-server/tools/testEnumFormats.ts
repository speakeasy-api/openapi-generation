/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { testEnumFormats } from "../../funcs/testEnumFormats.js";
import { TestEnumFormatsRequest$zodSchema } from "../../models/testenumformatsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TestEnumFormatsRequest$zodSchema,
};

export const tool$testEnumFormats: ToolDefinition<typeof args> = {
  name: "test-enum-formats",
  description: `Test x-speakeasy-enums in different formats

This endpoint tests the x-speakeasy-enums extension in both array and map formats,
including partial map coverage and both string and integer enum types.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await testEnumFormats(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
