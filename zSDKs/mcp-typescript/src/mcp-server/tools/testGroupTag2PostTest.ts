/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { testGroupTag2PostTest } from "../../funcs/testGroupTag2PostTest.js";
import { PostTest2Request$zodSchema } from "../../models/posttest2op.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostTest2Request$zodSchema,
};

export const tool$testGroupTag2PostTest: ToolDefinition<typeof args> = {
  name: "test-group-tag2-post-test",
  description: `Post Test2

This is a test endpoint.
It has a description.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await testGroupTag2PostTest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
