/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SDKCore } from "../core.js";
import { appendForm, encodeJSON, normalizeBlob } from "../lib/encodings.js";
import {
  bytesToBlob,
  getContentTypeFromFileName,
  readableStreamToArrayBuffer,
} from "../lib/files.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { APIError } from "../models/errors/apierror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import {
  PostFileWithEncodingRequest,
  PostFileWithEncodingRequest$zodSchema,
} from "../models/postfilewithencodingop.js";
import { APICall, APIPromise } from "../types/async.js";
import { isBlobLike } from "../types/blobs.js";
import { Result } from "../types/fp.js";
import { isReadableStream } from "../types/streams.js";

/**
 * Post File With Encoding
 *
 * @remarks
 * This endpoint tests the encoding field with multipart/form-data content type.
 * According to OpenAPI 3.0.3 spec, the encoding field is valid for both
 * application/x-www-form-urlencoded and multipart/* media types.
 *
 * This test includes multiple content types for the file field to verify
 * handling of comma-separated content types in encoding.
 */
export function tag1PostFileWithEncoding(
  client$: SDKCore,
  request: PostFileWithEncodingRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    Response,
    | APIError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >
> {
  return new APIPromise($do(
    client$,
    request,
    options,
  ));
}

async function $do(
  client$: SDKCore,
  request: PostFileWithEncodingRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      Response,
      | APIError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    APICall,
  ]
> {
  const parsed$ = safeParse(
    request,
    (value$) => PostFileWithEncodingRequest$zodSchema.parse(value$),
    "Input validation failed",
  );
  if (!parsed$.ok) {
    return [parsed$, { status: "invalid" }];
  }
  const payload$ = parsed$.value;
  const body$ = new FormData();
  if (isBlobLike(payload$.file)) {
    const file = payload$.file;
    const blob = await normalizeBlob(file);
    const name = "name" in file ? (file.name as string) : undefined;
    appendForm(body$, "file", blob, name);
  } else if (isReadableStream(payload$.file.content)) {
    const buffer = await readableStreamToArrayBuffer(payload$.file.content);
    const contentType = getContentTypeFromFileName(payload$.file.fileName)
      || "application/octet-stream";
    appendForm(
      body$,
      "file",
      bytesToBlob(buffer, contentType),
      payload$.file.fileName,
    );
  } else {
    const contentType = getContentTypeFromFileName(payload$.file.fileName)
      || "application/octet-stream";
    appendForm(
      body$,
      "file",
      bytesToBlob(payload$.file.content, contentType),
      payload$.file.fileName,
    );
  }
  if (payload$.attachment !== undefined) {
    if (isBlobLike(payload$.attachment)) {
      const file = payload$.attachment;
      const blob = await normalizeBlob(file);
      const name = "name" in file ? (file.name as string) : undefined;
      appendForm(body$, "attachment", blob, name);
    } else if (isReadableStream(payload$.attachment.content)) {
      const buffer = await readableStreamToArrayBuffer(
        payload$.attachment.content,
      );
      const contentType =
        getContentTypeFromFileName(payload$.attachment.fileName)
        || "application/octet-stream";
      appendForm(
        body$,
        "attachment",
        bytesToBlob(buffer, contentType),
        payload$.attachment.fileName,
      );
    } else {
      const contentType =
        getContentTypeFromFileName(payload$.attachment.fileName)
        || "application/octet-stream";
      appendForm(
        body$,
        "attachment",
        bytesToBlob(payload$.attachment.content, contentType),
        payload$.attachment.fileName,
      );
    }
  }
  if (payload$.fileName !== undefined) {
    appendForm(body$, "fileName", payload$.fileName);
  }
  if (payload$.filePurpose !== undefined) {
    appendForm(body$, "filePurpose", payload$.filePurpose);
  }
  if (payload$.metadata !== undefined) {
    appendForm(
      body$,
      "metadata",
      encodeJSON("metadata", payload$.metadata, { explode: true }),
    );
  }
  const path$ = pathToFunc("/fileWithEncoding")();

  const headers$ = new Headers(compactMap({
    Accept: "application/json",
  }));
  const securityInput = await extractSecurity(client$._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client$._options,
    baseURL: options?.serverURL ?? client$._baseURL ?? "",
    operationID: "postFileWithEncoding",
    oAuth2Scopes: null,
    resolvedSecurity: requestSecurity,
    securitySource: client$._options.security,
    retryConfig: options?.retries
      || client$._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || [
      "429",
      "500",
      "502",
      "503",
      "504",
    ],
  };

  const requestRes = client$._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: options?.serverURL,
    path: path$,
    headers: headers$,
    body: body$,
    userAgent: client$._options.userAgent,
    timeoutMs: options?.timeoutMs || client$._options.timeoutMs
      || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req$ = requestRes.value;

  const doResult = await client$._do(req$, {
    context,
    errorCodes: [],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req$ }];
  }
  return [doResult, {
    status: "complete",
    "request": req$,
    response: doResult.value,
  }];
}
