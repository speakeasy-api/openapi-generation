/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { SDKValidationError } from "../models/sdkvalidationerror.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";

/**
 * A car in the bar namespace
 */
export type Car = {
  wheels: 4;
  engineType: string;
};

/** @internal */
export const Car$inboundSchema: z.ZodMiniType<Car, unknown> = z.object({
  wheels: types.literal(4),
  engineType: types.string(),
});

export function carFromJSON(
  jsonString: string,
): SafeParseResult<Car, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Car$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Car' from JSON`,
  );
}
