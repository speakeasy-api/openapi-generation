/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  QueryClient,
  QueryFunctionContext,
  QueryKey,
} from "@tanstack/react-query";
import { SDKCore } from "../core.js";
import { getUser } from "../funcs/getUser.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as models from "../models/index.js";
import { unwrapAsync } from "../types/fp.js";
export type GetUserQueryData = models.User;

export function prefetchGetUser(
  queryClient: QueryClient,
  client$: SDKCore,
  id: string,
  options?: Omit<RequestOptions, "extraBody">,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildGetUserQuery(
      client$,
      id,
      options,
    ),
  });
}

export function buildGetUserQuery(
  client$: SDKCore,
  id: string,
  options?: Omit<RequestOptions, "extraBody">,
): {
  queryKey: QueryKey;
  queryFn: (context: QueryFunctionContext) => Promise<GetUserQueryData>;
} {
  return {
    queryKey: queryKeyGetUser(id),
    queryFn: async function getUserQueryFn(ctx): Promise<GetUserQueryData> {
      const sig = combineSignals(
        ctx.signal,
        options?.signal,
        options?.fetchOptions?.signal,
      );
      const mergedOptions = {
        ...options?.fetchOptions,
        ...options,
        signal: sig,
      };

      return unwrapAsync(getUser(
        client$,
        id,
        mergedOptions,
      ));
    },
  };
}

export function queryKeyGetUser(id: string): QueryKey {
  return ["openapi", "getUser", id];
}
