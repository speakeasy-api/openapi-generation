/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  QueryClient,
  QueryFunctionContext,
  QueryKey,
} from "@tanstack/react-query";
import { SDKCore } from "../core.js";
import { login } from "../funcs/login.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as models from "../models/index.js";
import { unwrapAsync } from "../types/fp.js";
export type LoginQueryData = models.LoginResponse;

export function prefetchLogin(
  queryClient: QueryClient,
  client$: SDKCore,
  options?: Omit<RequestOptions, "extraBody">,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildLoginQuery(
      client$,
      options,
    ),
  });
}

export function buildLoginQuery(
  client$: SDKCore,
  options?: Omit<RequestOptions, "extraBody">,
): {
  queryKey: QueryKey;
  queryFn: (context: QueryFunctionContext) => Promise<LoginQueryData>;
} {
  return {
    queryKey: queryKeyLogin(),
    queryFn: async function loginQueryFn(ctx): Promise<LoginQueryData> {
      const sig = combineSignals(
        ctx.signal,
        options?.signal,
        options?.fetchOptions?.signal,
      );
      const mergedOptions = {
        ...options?.fetchOptions,
        ...options,
        signal: sig,
      };

      return unwrapAsync(login(
        client$,
        mergedOptions,
      ));
    },
  };
}

export function queryKeyLogin(): QueryKey {
  return ["openapi", "login"];
}
