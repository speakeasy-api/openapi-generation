/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect } from "vitest";
import { SDK } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

export async function authenticate(): Promise<any> {
  const testHttpClient = createTestHTTPClient("authenticate");

  let sdk = new SDK({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    security: {
      option4: {
        appId: "app-speakeasy-123",
        secret: "MTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTI",
      },
    },
    httpClient: testHttpClient,
  });

  const loginResult = await sdk.login();
  expect(loginResult).toBeDefined();
  expect(loginResult?.token).not.toHaveLength(0);
  sdk = new SDK({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    security: {
      userPassAuth: {
        username: "<USERNAME>",
        password: "<PASSWORD>",
      },
    },
    httpClient: testHttpClient,
  });

  const validateResult = await sdk.validate();
  expect(validateResult).toBeDefined();
  expect(validateResult?.valid).toBeDefined();
  expect(validateResult?.valid).toEqual(true);

  return {
    token: loginResult?.token,
  };
}

export async function userSetup(): Promise<any> {
  const testHttpClient = createTestHTTPClient("userSetup");

  const sdk = new SDK({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
  });

  const createResult = await sdk.createUser({
    email: "Trystan_Crooks@hotmail.com",
    firstName: "Trystan",
    lastName: "Crooks",
    age: 32,
    postalCode: "94110",
    metadata: {
      allergies: "none",
      additionalProperties: {
        "color": "red",
        "height": "182",
        "weight": "77",
        "is_smoking": "true",
      },
    },
  });
  expect(createResult).toBeDefined();
  expect(createResult?.email).toEqual("Trystan_Crooks@hotmail.com");
  expect(createResult?.postalCode).toBeDefined();
  expect(createResult?.postalCode).toEqual("94110");

  return {
    user: createResult,
  };
}

export async function userTeardown(inputs: any) {
  const testHttpClient = createTestHTTPClient("userTeardown");

  const sdk = new SDK({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
  });

  await sdk.deleteUser(inputs.id);
}
