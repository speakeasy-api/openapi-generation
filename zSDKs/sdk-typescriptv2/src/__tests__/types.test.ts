/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { PetStatus } from "../bar/index.js";
import { PetSpecies } from "../foo/index.js";
import { SDK } from "../index.js";
import { createTestHTTPClient } from "./testclient.js";

test("Types Get Namespace Types", async () => {
  const testHttpClient = createTestHTTPClient("getNamespaceTypes");

  const sdk = new SDK({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
  });

  const result = await sdk.namespaceTests.types.getNamespaceTypes();
  expect(result).toBeDefined();
  expect(result).toEqual({
    fooSpecies: PetSpecies.Cat,
    barStatus: PetStatus.Available,
    fooAnimal: {
      animalType: "cat",
      furLength: "short",
    },
    barVehicle: {
      wheels: 4,
      engineType: "V8",
    },
    fooOrg: {
      name: "Acme Corp",
      address: {
        street: "123 Main St",
        city: "Springfield",
        location: {
          latitude: 37.7749,
          longitude: -122.4194,
        },
      },
      departments: [
        {
          deptName: "Engineering",
          headCount: 50,
        },
      ],
    },
  });
});

test("Types Get Namespace Animal", async () => {
  const testHttpClient = createTestHTTPClient("getNamespaceAnimal");

  const sdk = new SDK({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
  });

  const result = await sdk.namespaceTests.types.getNamespaceAnimal();
  expect(result).toBeDefined();
  expect(result).toEqual({
    animalType: "dog",
    breed: "Labrador",
  });
});

test("Types Get Namespace Vehicle", async () => {
  const testHttpClient = createTestHTTPClient("getNamespaceVehicle");

  const sdk = new SDK({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
  });

  const result = await sdk.namespaceTests.types.getNamespaceVehicle();
  expect(result).toBeDefined();
  expect(result).toEqual({
    wheels: 2,
    hasPedals: true,
  });
});

test("Types Get Namespace Organization", async () => {
  const testHttpClient = createTestHTTPClient("getNamespaceOrganization");

  const sdk = new SDK({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
  });

  const result = await sdk.namespaceTests.types.getNamespaceOrganization();
  expect(result).toBeDefined();
  expect(result).toEqual({
    name: "Acme Corp",
    address: {
      street: "123 Main St",
      city: "Springfield",
      location: {
        latitude: 37.7749,
        longitude: -122.4194,
      },
    },
    departments: [
      {
        deptName: "Engineering",
        headCount: 50,
      },
    ],
  });
});
