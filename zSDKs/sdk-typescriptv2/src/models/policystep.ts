/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  ApprovalConfig,
  ApprovalConfig$inboundSchema,
} from "./approvalconfig.js";
import { SDKValidationError } from "./sdkvalidationerror.js";

/**
 * A step in the policy
 */
export type PolicyStep = {
  stepId: string;
  /**
   * Approval configuration with escalation
   */
  approval: ApprovalConfig;
};

/** @internal */
export const PolicyStep$inboundSchema: z.ZodMiniType<PolicyStep, unknown> = z
  .object({
    stepId: types.string(),
    approval: ApprovalConfig$inboundSchema,
  });

export function policyStepFromJSON(
  jsonString: string,
): SafeParseResult<PolicyStep, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PolicyStep$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PolicyStep' from JSON`,
  );
}
