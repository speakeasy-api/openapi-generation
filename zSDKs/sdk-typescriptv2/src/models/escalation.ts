/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./sdkvalidationerror.js";

/**
 * Escalation configuration with string-encoded integer expiration
 */
export type Escalation = {
  enabled: boolean;
  /**
   * Expiration in seconds, encoded as a string in the API response
   */
  expiration: number;
  fallbackApprovers: Array<string>;
};

/** @internal */
export const Escalation$inboundSchema: z.ZodMiniType<Escalation, unknown> = z
  .object({
    enabled: types.boolean(),
    expiration: types.number(),
    fallbackApprovers: z.array(types.string()),
  });

export function escalationFromJSON(
  jsonString: string,
): SafeParseResult<Escalation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Escalation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Escalation' from JSON`,
  );
}
