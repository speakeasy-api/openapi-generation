/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { SDKCore } from "../core.js";
import { encodeFormQuery, encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/httpclienterrors.js";
import * as models from "../models/index.js";
import {
  ListTest1OpServerList,
  ListTest1OpServerMain,
} from "../models/listtest1op.js";
import { ResponseValidationError } from "../models/responsevalidationerror.js";
import { SDKBaseError } from "../models/sdkbaseerror.js";
import { SDKValidationError } from "../models/sdkvalidationerror.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";
import {
  createPageIterator,
  haltIterator,
  PageIterator,
  Paginator,
} from "../types/operations.js";

/**
 * Get Test1
 *
 * @remarks
 * This is a {{test}} endpoint.
 * It has a description.
 *
 * If set, this operation will use either {@link Security.myApiKey} or {@link Security.option6} from the global security.
 */
export function tag1ListTest1(
  client: SDKCore,
  page: number,
  queryParam2: models.QueryParam2,
  headerParam1: string,
  queryParam1?: string | undefined,
  options?: Omit<RequestOptions, "extraBody">,
): APIPromise<
  PageIterator<
    Result<
      models.ListTest1Response,
      | models.BadRequestResponseError
      | models.ErrorsError
      | SDKBaseError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    { page: number }
  >
> {
  return new APIPromise($do(
    client,
    page,
    queryParam2,
    headerParam1,
    queryParam1,
    options,
  ));
}

async function $do(
  client: SDKCore,
  page: number,
  queryParam2: models.QueryParam2,
  headerParam1: string,
  queryParam1?: string | undefined,
  options?: Omit<RequestOptions, "extraBody">,
): Promise<
  [
    PageIterator<
      Result<
        models.ListTest1Response,
        | models.BadRequestResponseError
        | models.ErrorsError
        | SDKBaseError
        | ResponseValidationError
        | ConnectionError
        | RequestAbortedError
        | RequestTimeoutError
        | InvalidRequestError
        | UnexpectedClientError
        | SDKValidationError
      >,
      { page: number }
    >,
    APICall,
  ]
> {
  const input: models.ListTest1Request = {
    page: page,
    queryParam2: queryParam2,
    headerParam1: headerParam1,
    queryParam1: queryParam1,
  };

  const parsed = safeParse(
    input,
    (value) => z.parse(models.ListTest1Request$outboundSchema, value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [haltIterator(parsed), { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const baseURL = options?.serverURL
    || pathToFunc(ListTest1OpServerList[ListTest1OpServerMain], {
      charEncoding: "percent",
    })();

  const pathParams = {
    page: encodeSimple("page", payload.page, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path = pathToFunc("/test1/{page}")(pathParams);

  const query = encodeFormQuery({
    "queryParam1": payload.queryParam1 ?? client._options.queryParam1,
    "queryParam2": payload.queryParam2,
  });

  const headers = new Headers(compactMap({
    Accept: "application/json",
    "headerParam1": encodeSimple("headerParam1", payload.headerParam1, {
      explode: false,
      charEncoding: "none",
    }),
  }));

  const securityInput = await extractSecurity(client._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput, [6, 5]);

  const context = {
    options: client._options,
    baseURL: baseURL ?? "",
    operationID: "listTest1",
    oAuth2Scopes: ["read", "write"],

    resolvedSecurity: requestSecurity,

    securitySource: client._options.security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: baseURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || 50000,
  }, options);
  if (!requestRes.ok) {
    return [haltIterator(requestRes), { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [haltIterator(doResult), { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result, raw] = await M.match<
    models.ListTest1Response,
    | models.BadRequestResponseError
    | models.ErrorsError
    | SDKBaseError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, models.ListTest1Response$inboundSchema, { key: "Result" }),
    M.jsonErr(400, models.BadRequestResponseError$inboundSchema),
    M.jsonErr(500, models.ErrorsError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [haltIterator(result), {
      status: "complete",
      request: req,
      response,
    }];
  }

  const nextFunc = (
    responseData: unknown,
  ): {
    next: Paginator<
      Result<
        models.ListTest1Response,
        | models.BadRequestResponseError
        | models.ErrorsError
        | SDKBaseError
        | ResponseValidationError
        | ConnectionError
        | RequestAbortedError
        | RequestTimeoutError
        | InvalidRequestError
        | UnexpectedClientError
        | SDKValidationError
      >
    >;
    "~next"?: { page: number };
  } => {
    const page = input?.page ?? 1;
    const nextPage = page + 1;

    if (!responseData) {
      return { next: () => null };
    }
    const results = (responseData as { resultArray: unknown }).resultArray;
    if (!Array.isArray(results) || !results.length) {
      return { next: () => null };
    }

    const nextVal = () =>
      tag1ListTest1(
        client,
        nextPage,
        queryParam2,
        headerParam1,
        queryParam1,
        options,
      );

    return { next: nextVal, "~next": { page: nextPage } };
  };

  const page$ = { ...result, ...nextFunc(raw) };
  return [{ ...page$, ...createPageIterator(page$, (v) => !v.ok) }, {
    status: "complete",
    request: req,
    response,
  }];
}
