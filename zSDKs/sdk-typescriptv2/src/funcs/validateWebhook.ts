/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SDKCore } from "../core.js";
import * as models from "../models/index.js";
import { SDKValidationError } from "../models/sdkvalidationerror.js";
import { ERR, Result } from "../types/fp.js";
import { WebhookAuthenticationError } from "../types/webhooks.js";

export async function validateWebhook(client: SDKCore, {
  request: rawRequest,
  secret,
}: {
  request: {
    body: BodyInit;
    method: string;
    url: string;
    headers: Record<string, string> | Headers;
  } | Request;
  secret: string;
}): Promise<
  Result<
    models.PaymentCreatedRequest | models.PaymentUpdatedRequest,
    SDKValidationError | WebhookAuthenticationError
  >
> {
  const request = normalizeRequest(rawRequest);
  const verifyResult = await client._verifyWebhook({ request, secret });
  if (!verifyResult.ok) {
    return verifyResult;
  }
  const jsonString = await request.text();
  const knownSchemas = [
    models.paymentCreatedRequestFromJSON,
    models.paymentUpdatedRequestFromJSON,
  ];

  for (const schema of knownSchemas) {
    const ret = schema(jsonString);
    if (ret.ok) {
      return ret;
    }
  }

  return ERR(
    new SDKValidationError(
      "No matching schema found for the given webhook payload",
      jsonString,
      jsonString,
    ),
  );
}

function normalizeRequest(
  request: {
    body: BodyInit;
    method: string;
    url: string;
    headers: Record<string, string> | Headers;
  } | Request,
): Request {
  if (request instanceof Request) {
    return request;
  }
  return new Request(request.url, request);
}
