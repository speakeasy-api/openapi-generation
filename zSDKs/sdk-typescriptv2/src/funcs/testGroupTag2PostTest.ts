/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { SDKCore } from "../core.js";
import * as b64$ from "../lib/base64.js";
import { encodeFormQuery, encodeJSON } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/httpclienterrors.js";
import * as models from "../models/index.js";
import {
  PostTest2OpServerDefault,
  PostTest2OpServerList,
} from "../models/posttest2op.js";
import { ResponseValidationError } from "../models/responsevalidationerror.js";
import { SDKBaseError } from "../models/sdkbaseerror.js";
import { SDKValidationError } from "../models/sdkvalidationerror.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";
import * as types$ from "../types/primitives.js";

/**
 * Post Test2
 *
 * @remarks
 * This is a test endpoint.
 * It has a description.
 *
 * If set, this operation will use {@link Security.myApiKey} from the global security.
 */
export function testGroupTag2PostTest(
  client: SDKCore,
  test2Request: models.Test2Request,
  deprecatedQueryParam1?: string | undefined,
  deprecatedQueryParam2?: string | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    Uint8Array | undefined,
    | models.BadRequestResponseError
    | models.ErrorsError
    | models.Test2ResponseError
    | SDKBaseError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    test2Request,
    deprecatedQueryParam1,
    deprecatedQueryParam2,
    options,
  ));
}

async function $do(
  client: SDKCore,
  test2Request: models.Test2Request,
  deprecatedQueryParam1?: string | undefined,
  deprecatedQueryParam2?: string | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      Uint8Array | undefined,
      | models.BadRequestResponseError
      | models.ErrorsError
      | models.Test2ResponseError
      | SDKBaseError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const input: models.PostTest2Request = {
    test2Request: test2Request,
    deprecatedQueryParam1: deprecatedQueryParam1,
    deprecatedQueryParam2: deprecatedQueryParam2,
  };

  const parsed = safeParse(
    input,
    (value) => z.parse(models.PostTest2Request$outboundSchema, value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = encodeJSON("body", payload.Test2Request, { explode: true });

  const baseURL = options?.serverURL
    || pathToFunc(PostTest2OpServerList[PostTest2OpServerDefault], {
      charEncoding: "percent",
    })({
      port: "35123",
      hostname: "localhost",
    });

  const path = pathToFunc("/test2")();

  const query = encodeFormQuery({
    "deprecatedQueryParam1": payload.deprecatedQueryParam1
      ?? client._options.deprecatedQueryParam1,
    "deprecatedQueryParam2": payload.deprecatedQueryParam2
      ?? client._options.deprecatedQueryParam2,
  });

  const headers = new Headers(compactMap({
    "Content-Type": "application/json",
    Accept: "multipart/form-data",
  }));

  const securityInput = await extractSecurity(client._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput, [6]);

  const context = {
    options: client._options,
    baseURL: baseURL ?? "",
    operationID: "postTest2",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || {
        strategy: "backoff",
        backoff: {
          initialInterval: 10,
          maxInterval: 200,
          exponent: 1.5,
          maxElapsedTime: 1000,
        },
        retryConnectionErrors: false,
      }
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["404"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: baseURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    Uint8Array | undefined,
    | models.BadRequestResponseError
    | models.ErrorsError
    | models.Test2ResponseError
    | SDKBaseError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.bytes(200, types$.optional(b64$.zodInbound), {
      ctype: "multipart/form-data",
    }),
    M.jsonErr(400, models.BadRequestResponseError$inboundSchema),
    M.jsonErr(404, models.ErrorsError$inboundSchema),
    M.jsonErr(500, models.Test2ResponseError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
    M.nil("default", types$.optional(b64$.zodInbound)),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
