/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package org.openapis.openapi.models.foo;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * PetSpecies
 *
 * <p>Species of a pet in the foo namespace
 */
public class PetSpecies {

    public static final PetSpecies DOG = new PetSpecies("dog");
    public static final PetSpecies CAT = new PetSpecies("cat");
    public static final PetSpecies BIRD = new PetSpecies("bird");
    public static final PetSpecies FISH = new PetSpecies("fish");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, PetSpecies> values = createValuesMap();
    private static final Map<String, PetSpeciesEnum> enums = createEnumsMap();

    private final String value;

    private PetSpecies(String value) {
        this.value = value;
    }

    /**
     * Returns a PetSpecies with the given value. For a specific value the
     * returned object will always be a singleton so reference equality
     * is satisfied when the values are the same.
     *
     * @param value value to be wrapped as PetSpecies
     */
    @JsonCreator
    public static PetSpecies of(String value) {
        synchronized (PetSpecies.class) {
            return values.computeIfAbsent(value, v -> new PetSpecies(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<PetSpeciesEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj) return true;
        if (obj == null) return false;
        if (getClass() != obj.getClass()) return false;
        PetSpecies other = (PetSpecies) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "PetSpecies [value=" + value + "]";
    }

    // return an array just like an enum
    public static PetSpecies[] values() {
        synchronized (PetSpecies.class) {
            return values.values().toArray(new PetSpecies[] {});
        }
    }

    private static final Map<String, PetSpecies> createValuesMap() {
        Map<String, PetSpecies> map = new LinkedHashMap<>();
        map.put("dog", DOG);
        map.put("cat", CAT);
        map.put("bird", BIRD);
        map.put("fish", FISH);
        return map;
    }

    private static final Map<String, PetSpeciesEnum> createEnumsMap() {
        Map<String, PetSpeciesEnum> map = new HashMap<>();
        map.put("dog", PetSpeciesEnum.DOG);
        map.put("cat", PetSpeciesEnum.CAT);
        map.put("bird", PetSpeciesEnum.BIRD);
        map.put("fish", PetSpeciesEnum.FISH);
        return map;
    }

    public enum PetSpeciesEnum {

        DOG("dog"),
        CAT("cat"),
        BIRD("bird"),
        FISH("fish"),
        ;

        private final String value;

        private PetSpeciesEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}
