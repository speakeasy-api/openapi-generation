/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package org.openapis.openapi.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Long;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * IntEnum
 *
 * <p>An integer enum property.
 */
public class IntEnum {

    public static final IntEnum First = new IntEnum(1L);
    public static final IntEnum Second = new IntEnum(2L);
    public static final IntEnum Third = new IntEnum(3L);

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<Long, IntEnum> values = createValuesMap();
    private static final Map<Long, IntEnumEnum> enums = createEnumsMap();

    private final long value;

    private IntEnum(long value) {
        this.value = value;
    }

    /**
     * Returns a IntEnum with the given value. For a specific value the
     * returned object will always be a singleton so reference equality
     * is satisfied when the values are the same.
     *
     * @param value value to be wrapped as IntEnum
     */
    @JsonCreator
    public static IntEnum of(long value) {
        synchronized (IntEnum.class) {
            return values.computeIfAbsent(value, v -> new IntEnum(v));
        }
    }

    @JsonValue
    public long value() {
        return value;
    }

    public Optional<IntEnumEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj) return true;
        if (obj == null) return false;
        if (getClass() != obj.getClass()) return false;
        IntEnum other = (IntEnum) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "IntEnum [value=" + value + "]";
    }

    // return an array just like an enum
    public static IntEnum[] values() {
        synchronized (IntEnum.class) {
            return values.values().toArray(new IntEnum[] {});
        }
    }

    private static final Map<Long, IntEnum> createValuesMap() {
        Map<Long, IntEnum> map = new LinkedHashMap<>();
        map.put(1L, First);
        map.put(2L, Second);
        map.put(3L, Third);
        return map;
    }

    private static final Map<Long, IntEnumEnum> createEnumsMap() {
        Map<Long, IntEnumEnum> map = new HashMap<>();
        map.put(1L, IntEnumEnum.First);
        map.put(2L, IntEnumEnum.Second);
        map.put(3L, IntEnumEnum.Third);
        return map;
    }

    public enum IntEnumEnum {

        First(1L),
        Second(2L),
        Third(3L),
        ;

        private final long value;

        private IntEnumEnum(long value) {
            this.value = value;
        }

        public long value() {
            return value;
        }
    }
}
