/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package org.openapis.openapi.models.shared;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class Icon {

    public static final Icon TICK = new Icon("tick");
    public static final Icon THUMBS_UP = new Icon("thumbs-up");
    public static final Icon FIRE = new Icon("fire");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Icon> values = createValuesMap();
    private static final Map<String, IconEnum> enums = createEnumsMap();

    private final String value;

    private Icon(String value) {
        this.value = value;
    }

    /**
     * Returns a Icon with the given value. For a specific value the
     * returned object will always be a singleton so reference equality
     * is satisfied when the values are the same.
     *
     * @param value value to be wrapped as Icon
     */
    @JsonCreator
    public static Icon of(String value) {
        synchronized (Icon.class) {
            return values.computeIfAbsent(value, v -> new Icon(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<IconEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj) return true;
        if (obj == null) return false;
        if (getClass() != obj.getClass()) return false;
        Icon other = (Icon) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Icon [value=" + value + "]";
    }

    // return an array just like an enum
    public static Icon[] values() {
        synchronized (Icon.class) {
            return values.values().toArray(new Icon[] {});
        }
    }

    private static final Map<String, Icon> createValuesMap() {
        Map<String, Icon> map = new LinkedHashMap<>();
        map.put("tick", TICK);
        map.put("thumbs-up", THUMBS_UP);
        map.put("fire", FIRE);
        return map;
    }

    private static final Map<String, IconEnum> createEnumsMap() {
        Map<String, IconEnum> map = new HashMap<>();
        map.put("tick", IconEnum.TICK);
        map.put("thumbs-up", IconEnum.THUMBS_UP);
        map.put("fire", IconEnum.FIRE);
        return map;
    }

    public enum IconEnum {

        TICK("tick"),
        THUMBS_UP("thumbs-up"),
        FIRE("fire"),
        ;

        private final String value;

        private IconEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}
