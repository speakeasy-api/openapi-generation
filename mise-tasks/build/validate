#!/usr/bin/env bash
#MISE description="Build the OpenAPI validator"

set -euo pipefail

readonly ROOT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." &>/dev/null && pwd)"
cd "$ROOT_DIR"

mkdir -p ./bin
go build -o ./bin/validate ./cmd/validate/main.go
