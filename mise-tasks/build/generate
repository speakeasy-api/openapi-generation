#!/usr/bin/env bash
#MISE description="Build the SDK generator"

set -euo pipefail

readonly ROOT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." &>/dev/null && pwd)"
cd "$ROOT_DIR"

mkdir -p ./bin
go build -o ./bin/generate ./cmd/generate/main.go
